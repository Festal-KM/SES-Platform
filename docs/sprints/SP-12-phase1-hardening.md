# SP-12 phase1-hardening — Phase 1 の完了判定（負荷・E2E・リリース条件）

> **Phase**: 1（MVP。**最終スプリント**） / **前提**: SP-09 / SP-10 / SP-11 / **SP-21**（UI 基盤。§4 T-12-11） / **後続**: SP-13（Phase 2）
> 🔴 **2026-09-17（2 回目の追記）**: SP-10 / SP-11 の完了確認（同日。いずれも `PHASE_INCOMPLETE`。**理由の大半は計画側の転記漏れ**）で挙がった NG を本スプリントに転記した —— **`T-12-17`**（SP-10 / SP-11 からのコード・テスト是正。`T-12-13` と同型）/ **`T-12-18`**（上流改訂・人間判断を要するもの。`T-12-14` と同型）を新設し、`T-12-09` の完了判定に ⑤（SNS の申請。[Issue #62](https://github.com/Festal-KM/SES-Platform/issues/62)）を足し、`T-12-03` の証跡表に #26（`A-012` と **`F-053 AC-3` 実演の通し**）を加えた（`docs/dev-plan.md` §8 の 2026-09-17 の行）。**16 → 18 タスク。**
> 🔴 **本スプリントの完了をもって「第 1 回リリース」を行う**（2026-09-10、人間の決定。[Issue #46](https://github.com/Festal-KM/SES-Platform/issues/46)「Phase 1、Phase 2、Phase 3 のそれぞれでリリースして」）。**リリースは 3 回あり、そのうち本番環境をゼロから立てるのは第 1 回だけである。準備タスクは `T-12-11`**（`docs/dev-plan.md` §2.2 / §3.2 / §5 E-16）。
> **一次資料**: `CLAUDE.md` §5 / §7 / `docs/02` 章 7.1 / 章 7.6 / 章 7.7 / 章 8.7 / `docs/03` §3.7.2 / `docs/03` `pm` 申し送り 5・8・14 / `docs/05` §13.6 / §17.3 / §17.4 / §17.6
> **完了確認**: `MODE: REVIEW` / `TARGET: SP-12` → 続けて `TARGET: Phase 1`

---

## 1. 目的

🔴 **`CLAUDE.md` §5 の Phase 1 成功条件 3 つと、§2.1 のリリース条件 R-1 / R-2 を、証跡（テスト名 + 実行ログ）とともに満たす。** 実装がすべて終わっていても、この 5 つが揃わなければ Phase 1 は閉じない。

⚠️ **例外が 1 つある: `T-12-10`（[Issue #42](https://github.com/Festal-KM/SES-Platform/issues/42) = 回答②）は実装を伴う** —— 公開後の公開欄の編集で再検査し、FAIL なら公開を解除する。**判断を人間に仰いだ結果、実装が確定したためである**（2026-09-10）。それ以外は新機能を実装しない。**負荷・E2E・環境分離・縮退・人間判断の 5 領域を仕上げる。**

⚠️ **2026-09-17 の追記（SP-09 の完了確認。`docs/sprints/SP-09-proposal-flow.md` §8 / `docs/dev-plan.md` §8 の 2026-09-17 の行）**: **`T-12-13`〜`T-12-16` は「新機能」ではなく、Phase 1 で予定されていたのに持ち主が無かったもの**である —— ①SP-09 の T-09-09 ③ / T-09-10 ③ / T-09-11 ① が「SP-12 へ」と申し送った 11 項目（`T-12-13` に 6 / `T-12-14` に 4 / `T-12-15` に 1〔T-09-09 ③-④〕。加えて T-09-11 ② の E2E #23 前半を `T-12-14` ⑤ に）②`S-003` / `S-004` の要対応キュー（SP-03 T-03-06 が「Phase 1」と明記。`T-12-15`）③`docs/05` §6.5 #46b の差分ビューと `S-006` セクション 4・5（「SP-09」と書かれたまま SP-09 に持ち主が無かった。`T-12-16`）。🔴 **申し送りを置いたまま実行タスクが無い状態を作らない**（`docs/dev-plan.md` §8 の 2026-09-11 / 2026-09-15 の行と同型）。**同日の 2 回目の追記（SP-10 / SP-11 の完了確認）で `T-12-17` / `T-12-18` を同型で新設した** —— ④SP-10 / SP-11 の各タスクの決着行・レビュー申し送りが「SP-12 へ」と書いたコード / テストの是正 21 項目（`T-12-17`）⑤上流改訂・人間判断を要する 18 項目（`T-12-18`）。**どちらも新機能ではない。** 🔴 **環境分離には、SP-01 から引き継いだ E-2（AWS アカウント分離。T-12-09。🔴 `sandbox` 用アカウントは Phase 2 へ）を含む** — 実装で塞げるのは送信経路であり、**本番キーの混入はアカウントが分かれていて初めて構成として塞がる**（`docs/dev-plan.md` §5 E-2）。

## 2. 対応機能 ID

横断（`F-009` の性能 / `F-020`・`F-022` の縮退 / `F-053`・~~`F-054`~~・`F-064` の環境分離）

🔴 **`F-054`（`sandbox` テナントの期限管理と移行）は 2026-09-10 に Phase 2 へ移った**（[Issue #20](https://github.com/Festal-KM/SES-Platform/issues/20) = 「Production と demo だけでよい」）。**実施は `T-10-08`（ID 据え置き）で、環境の構築 `T-13-11` と同じスプリント（SP-13）。** 本スプリントで立てる環境は **`development` + `demo` + `production`** である。

## 3. タスク一覧

| ID | 概要 | 受け入れ基準（要旨） | 対応 | 工数 |
|---|---|---|---|---|
| T-12-01 | `seed:perf`（1 万 / 1 万 / 匿名共有 2,000） | 30 テナント。最大テナントにエンジニア 3,000 / 案件 3,000 / パートナー 15 社 | `docs/03` §3.7.2 | M |
| T-12-02 | 🔴 **負荷テスト（`F-009` の p95 1 秒）= R-2** | 🔴 満たせない場合は OpenSearch の要否を人間に提起する | `CLAUDE.md` §7 / R-2 / R-07 | L |
| T-12-03 | E2E の総仕上げ（Phase 1 の全シナリオ） | `docs/05` §17.3 の Phase 1 該当分がすべて green | `CLAUDE.md` §5 | L |
| T-12-04 | 環境分離の総合検証（NFR-ENV-1〜10） | 🔴 **`production` 以外から実在の宛先へ 0 通**。起動失敗 2 ケース | `BR-45` `BR-46` | M |
| T-12-05 | AI 全停止時の縮退（E2E #19） | AI が全停止でも承認・送信が成立し、ゲートは安全側に止まる | `docs/02` 章 8.7 | M |
| T-12-06 | 🔴 **R-1 リリース条件（匿名 5 項目の丸め方の承認）** | 🔴 **人間の承認が無ければ `PHASE_INCOMPLETE`** | `CLAUDE.md` §5 / §9-13 / E-9 | S |
| T-12-07 | 席単価・決済手数料率の再提起（E-5） | 論点が Issue に再掲され、回答期限が設定される | `docs/03` `pm` 申し送り 14 | S |
| T-12-08 | 固定費の実額確定と `docs/03` の更新（E-14） | `docs/03` §7.4 のレンジが実額に置き換わる | `docs/03` `pm` 申し送り 8 | S |
| T-12-09 | 🔴 **本番 / `sandbox` の AWS アカウント分離**（E-2）+ **GuardDuty スキャン所要時間の実測**（E-13） | 🔴 本番と `sandbox` が**別 AWS アカウント**に分かれ、非本番の認証情報で本番リソースへ到達できない。**GuardDuty のスキャン所要時間が実測され記録されている** | `docs/03` §3.2.8 / E-2 / E-13 | M |
| T-12-10 | 🔴 **公開後の公開欄の編集と再検査（[Issue #42](https://github.com/Festal-KM/SES-Platform/issues/42)）の決着** | 決定が記録され、推奨（再検査）を採る場合は実装とテストまで完了している | `F-014 AC-3` / `F-020` | S〜M |
| 🔴 **T-12-11** | 🔴 **第 1 回リリースの準備**（[Issue #46](https://github.com/Festal-KM/SES-Platform/issues/46)。**リリースに何が要るかの洗い出し自体がこのタスクの一部である**） | 🔴 **§4 の受け入れ基準 ①〜⑨ をすべて満たす。** 本番環境・`production` の起動時検証・障害対応手順・**手動請求の算出根拠**・**契約書はメール添付という制約の明示**・SP-21 の完了 | `CLAUDE.md` §11 / §10.4 / `docs/dev-plan.md` §2.2 / §5 E-16 | L |
| T-12-12 | 🔴 **メール / ストレージのクォータ上書きの執行点配線**（T-11-02 NG-1 の後続） | 執行点（`email-send.ts` / `send-hold-release.ts` / `issueSkillSheetUploadUrl`）が `tenant_quota_overrides` の上書き行を読み、上限の上げ下げが実際の送信・アップロードの挙動に反映される | `docs/05` §5.2 / §5.8.1 ⑧ | M |
| T-12-13 | 🔴 **SP-09 からの申し送りの回収 ①（上流改訂・人間判断を要しない整備）** | `audit_logs.target_type` が `PROPOSAL_AUDIT_TARGET_TYPE` に統一され既存行が移行済み / `docs/05` §6.5「5 本」→ 6 本 / `S-021` が再送の確定（`SUBMITTED` / `SUBMIT_FAILED`）を読み直し無しに拾う / `data-can-approve-now` / `S-024` の TZ と履歴 3 件の注記 | SP-09 T-09-09 ③-① / T-09-10 ③-①②③ / T-09-11 ①-①② | M |
| T-12-14 | 🔴 **SP-09 からの申し送りの回収 ②（上流改訂・人間判断を要するもの）** | `S-019` 一括承認の判断が Issue に起票され既定で進む / `S-023` ゲート結果が実行ごとの履歴で描かれる（#40 の拡張）/ `S-023` 履歴の折りたたみ方針が `docs/04` にある / 取引先の履歴の承認者名が Issue に起票され既定で進む / E2E #23 前半の扱いが T-12-03 の表に固定 | SP-09 T-09-09 ③-②③⑤ / T-09-11 ①-③ / T-09-11 ② | M |
| T-12-15 | 🔴 **`S-003` / `S-004` の要対応キュー（Phase 1 分）** | 送信失敗 / 承認待ち / ゲート差し戻し / 提案依頼の返答待ち / 送信保留 が `HomeBlock` に**追加専用**で載る。🔴 4 つの「うまくいかなかった」が混ざらず、`PROVIDER_QUOTA` の保留と `LOST` / `DECLINED` は載らず、取引先に他社由来の値が 0 件 | `F-006` / `docs/04` §S-003 §S-004 / SP-03 T-03-06 / SP-09 T-09-08 ②-④ / T-09-09 ③-④ | M |
| T-12-16 | 🔴 **#46b `GET /api/proposals/{id}/snapshot-diff` と `S-006` セクション 4（提案履歴）・5（凍結情報との差分）** | 凍結側と現在値が**左右に並置**され 1 つのリストに混在しない。🔴 **現在値を C3 で読めない提案では 404**（経路 2 の範囲を超えない）。`engineer-detail-proposals-coming-soon` が消える。先に `docs/05` #46b の「SP-09」を「SP-12」に改訂 | `F-019 AC-2` / `docs/05` §6.5 #46b / §17.3 #25 / `docs/04` §S-006 | M |
| 🔴 **T-12-17** | 🔴 **SP-10 / SP-11 からの申し送りの回収 ①（コード / テストの是正。上流改訂・人間判断を要しないもの）** | T-11-11 レビュー低 3 点 / `S-015` 氏名セルの `NameCell` / T-11-04 レビュー ①〜⑤ / `usage-storage-reconcile` の前月キー / `A-004`・`S-041` のモバイル E2E / `admin-forbidden-keys` の API-A8 例外を `^GateStall` に + `*Response` 型の走査 / `PROPOSAL_REQUEST_OPERATIONS` の書き込み側 2 箇所 / `TenantPurgeRun` `RUNNING` 滞留を `A-005` 項目 7 に / [#68](https://github.com/Festal-KM/SES-Platform/issues/68) の 2 点（CSV 無害化・`TENANT_PURGE_GRACE_DAYS` の本番固定）/ [#67](https://github.com/Festal-KM/SES-Platform/issues/67) B（回答後）/ ESLint `JSXText`（低優先）。**判定を緩めない・許可リストを広げない** | SP-10 T-10-01 ① / T-10-02→T-11-04 ④ / T-10-09 ② / SP-11 T-11-04 / T-11-05 ⑦ / T-11-07 ②⑥⑦ / T-11-09 ②⑤⑨ / T-11-11 / [#67](https://github.com/Festal-KM/SES-Platform/issues/67) / [#68](https://github.com/Festal-KM/SES-Platform/issues/68) | M |
| 🔴 **T-12-18** | 🔴 **SP-10 / SP-11 からの申し送りの回収 ②（上流改訂・人間判断を要するもの）** | `docs/04` §A-002 / §A-004 / §A-006 / §A-012 / §S-041 / §S-016 / §10.3 / §10.1〔用語〕の改訂と、`docs/05` §6.4 許可リスト表 → `pick-detail.ts` の順序を守る。GRANT が残る自由記述・名称列の要否 / 群 D の扱い / `S-041` CSV / `admin.` 接尾辞族 / `withPlatformWrite` 64 文字超 / `auth.login_failed` の `reason` / `demo` 項目 13 が 0 件 / 用語の揺れ / ワイヤーフレーム再生成（任意）。🔴 **`ui-design` / `program-design` が先。改訂前に画面・許可リストを触らない** | SP-10 T-10-01 ② / T-10-09 ③ / SP-11 T-11-01 ②③④ / T-11-02 ⑤ / T-11-03 ①③④ / T-11-07 ①③④⑧ / T-11-09 ①④⑥⑦ / T-11-12 / `docs/04` 申し送り 15 / [#63](https://github.com/Festal-KM/SES-Platform/issues/63) | M |

## 4. タスク詳細

🔴 **実行順（番号は採番順であって実行順ではない。`docs/dev-plan.md` `PM-A-11`）**: ①**上流改訂を伴うもの（`T-12-14` / `T-12-16` 手順 1 / `T-12-18`）の `ui-design` / `program-design` への依頼を最初に出す**（改訂待ちで実装が止まらないように）→ ②実装・是正（`T-12-10` / `T-12-12` / `T-12-13` / `T-12-15`〔済〕/ `T-12-16` / **`T-12-17`** / `T-12-18` の実装部分）→ ③`T-12-01` → `T-12-02`（負荷）→ ④`T-12-09` → `T-12-04`（環境分離。この順は固定）→ ⑤**`T-12-03`（E2E の総仕上げ。①〜④で足した E2E をここで全部回す）**→ `T-12-05` → ⑥非コード（`T-12-06` / `T-12-07` / `T-12-08`）→ ⑦`T-12-11`（第 1 回リリース準備。最後）。**`T-12-17` / `T-12-18` は `T-12-03` より前**（追加した spec / 読み替えを `T-12-03` の実行ログに含めるため）。

### T-12-01 `seed:perf`（M）

- **実装**: `packages/db/seed/presets/perf.ts`（`docs/05` §13.6 / `docs/03` §3.7.2）。
- **規模**: 30 テナント。**最大テナントにエンジニア 3,000 / 案件 3,000 / パートナー 15 社。** 全体でエンジニア 1 万件 / 案件 1 万件 / **匿名共有 2,000 件**。
- 🔴 **時系列データを含む**（`T` からの相対日）。**固定シードの疑似乱数で同じ入力から同じデータ。**
- 🔴 **合成データのみ。本番の顧客データを持ち出さない**（`BR-47`）。
- **完了の判定**: `pnpm seed --preset=perf --reset` が冪等に再生成でき、規模が仕様どおり。

### T-12-02 🔴 負荷テスト（R-2）（L）

- **実装**: `docs/05` §17.3 #20 / `docs/03` `pm` 申し送り 5 / `docs/03` §3.7.2。
- 🔴 **SP-06 の完了判定が「`F-009`（`AC-4` を除く）」「`F-015`（`AC-2` を除く）」として本タスクへ送っている分である**（SP-06 §6-1。`seed:perf` が T-12-01 で作られるため、SP-06 には測定対象の母集団が存在しない）。**この 2 つの AC は本タスクでしか green にならない。**
- **測定対象**:
  | # | 指標 | 目標 | 出典 |
  |---|---|---|---|
  | 1 | エンジニア 1 万件での複合検索（`F-009`。**匿名候補 2,000 件を含む**） | **p95 1 秒以内** | `CLAUDE.md` §7 / `F-009 AC-4` |
  | 2 | 案件 1 万件での検索（`F-015`） | **p95 1 秒以内** | `F-015 AC-2` |
  | 3 | ゲート実行（`gate.run`） | p95 30 秒 | `docs/02` 章 7.1 / `A-18` |
  | 4 | 送信ジョブ（`send.proposal`） | p95 60 秒 | 同上 |
- 🔴 **匿名候補が混在する検索を含めて評価する**（`docs/02` `tech-selection` 申し送り 3）。「自社スコープ」「共有スコープ」の 2 本のクエリ + アプリ層マージ（SP-08 の T-08-05）の実測。
- 🔴 **満たせない場合**: 検索基盤の代替（OpenSearch）の要否を**人間に提起する**（Issue を起票）。**実装は `packages/db/src/search/*.ts` の 1 箇所に閉じているため、差し替えの影響範囲が限定される**（SP-06 の T-06-05）。
- 🔴 **測定結果（p50 / p95 / p99・実行日時・シード条件）を `docs/dev-plan.md` §8 に記録する。** これが Phase 1 完了確認の証跡になる。
- **完了の判定**: 1 / 2 が目標を満たす、または未達の理由と対応方針が Issue として起票されている。

### T-12-03 E2E の総仕上げ（L）

- **実装**: `docs/05` §17.3 の全 ~~24~~ ~~25~~ **26** シナリオのうち、**Phase 1 に該当する ~~18~~ ~~19~~ 20 シナリオ**（= 26 − 除外 6 件）をすべて green にする。**下表の 20 行がこのタスクの証跡リストそのものであり、Phase 1 完了判定の入力になる。** ⚠️ **2026-09-17 に改訂（1 回目）**: `docs/05` §17.3 は SP-09 の `T-09-12` で **#25**（経験内容の行単位の凍結 + 差分ビュー。`F-008 AC-5` / `AC-6` / `F-019 AC-2` / `AC-5` = Phase 1）が追加され 25 行になっていたが、本表が 24 行のまま置かれていた。**件数の契約（下記 🔴）を守るために 19 行目として加える。差分ビュー部分の持ち主は `T-12-16`。** ⚠️ **2026-09-17 に改訂（2 回目。SP-10 の完了確認）**: SP-10 の `T-10-07` で **#26**（`A-012` のリセット導線。`F-053 AC-2` / `AC-6` = Phase 1）が追加され 26 行になっていた。**20 行目として加える。** あわせて **`F-053 AC-3`（実演の通し）の E2E** —— SP-10 T-10-06 / T-10-07 が「未着手。`tests/e2e/admin-demo.spec.ts` に `UC-21` の順路を挿し込む形で足せる」と申し送ったもの —— を **同じ #26 の行の証跡に含める**（✅ **2026-09-17 に実装済み = コミット `7cfe4d5`。`admin-demo.spec.ts` 4 件〔① 投入 / ② UC-21 シナリオ A / ③ UC-21 シナリオ B / ④ リセット〕。ローカル 8/8 passed**。CI の green 確認は本タスク）。
  | # | シナリオ | 実装スプリント |
  |---|---|---|
  | 1 | 2 テナント × 2 パートナーで越境 0 件 | SP-03 |
  | 2 | パートナーの全画面・API・集計・通知に他社由来の値が 0 件（件数バッジ・並び順・示唆も 0 件） | SP-03 / SP-06 |
  | 3 | 案件公開 → 提案 → ゲート → 承認 → 送信 → 結果記録 | SP-09 |
  | 4 | 🔴 ゲート FAIL の提案が送信できない | SP-09 |
  | 5 | 🔴 匿名候補が 5 項目でのみ現れ、`Proposal` 作成まで実名等に到達できない | SP-08 |
  | 6 | 同一候補が複数案件で参照子が異なり突合できない | SP-08 |
  | 7 | 送信を 2 回起動しても外部呼び出しが 1 回 | SP-09 |
  | 8 | 応答不明 → `SUBMIT_FAILED` → 自動再送されない → 人手再送で 1 回 | SP-09 |
  | 9 | 保留（ドメイン未検証）が `SUBMIT_FAILED` にならず、検証後に自動復帰 | SP-04 / SP-09 |
  | 10 | 承認後に本文を変更すると承認が無効になる | SP-09 |
  | 13 | 🔴 モバイルビューポートでの承認 | SP-09 |
  | 15 | 運営者に非開示のものが管理平面のどの応答にも現れない | SP-11 |
  | 16 | 削除完了の確認が `A-010` の 1 本からしか取れない | SP-10 |
  | 17 | `CLOSING` → 30 日 → `PURGED` で到達経路が 0 件 | SP-10 |
  | 18 | プロンプトインジェクションでゲートの判定が変わらない | SP-07 |
  | 20 | 負荷（T-12-02） | SP-12 |
  | 23 | AI 上限とゲート / ドメイン未検証の招待の自動送達 / 🔴 **送信基盤クォータ（`HELD_PROVIDER_QUOTA` と `send.*` の `PROVIDER_QUOTA`）** | SP-07 / SP-04 / SP-09 |
  | 24 | 🔴 **削除予告と環境枠**（予告が `HELD_PROVIDER_QUOTA` の間は `PURGED` にならず、`send.hold-release` 後に初めて削除される） | SP-10（T-10-12） |
  | 25 | 🔴 **経験内容の行単位の凍結**（台帳で 1 行編集・1 行削除・1 行追加しても `S-023` の行数と 4 項目が変わらない）**+ 差分ビューで凍結側と現在値が左右に並置され同一リストに混在しない**（2026-09-17 に追加）。✅ **読み替え（T-12-16。2026-09-17）**: 差分ビュー部分は **E2E `audit-k7.spec.ts` ⑤**（`S-006` → セクション 4 → 選択 → セクション 5。凍結側の表と現在値の表が別で互いの行が混ざらない + `engineer.view` が `DETAIL` / `SNAPSHOT_DIFF` で残る）+ **結合 `tests/isolation/proposal-snapshot-diff.test.ts`** ②③。凍結側の不変は結合 `proposals-create-update.test.ts` のまま（`T-12-03` はこの 3 本を実行ログに含める） | SP-09（T-09-12。凍結）/ **SP-12（T-12-16。差分ビュー）** |
  | 26 | 🔴 **`A-012` のリセット導線**（投入 → 確認ステップ〔環境名 + テナント名の両方が一致するまで実行ボタン無効〕→ リセット → 冪等。`F-053 AC-2` / `AC-6`。**`production` / `sandbox` / `staging` の「導線が無く API が 403」は結合 `tests/isolation/seed-demo.test.ts` ⑤ / ⑦ + render の読み替え** —— ハーネスは `APP_ENV=development` 固定）**+ 🔴 `F-053 AC-3` 実演の通し**（投入直後に `UC-21` の順路 `S-013` → `S-020` → `S-021` → `S-023` → `S-016` を通す。**✅ 2026-09-17 に実装済み `7cfe4d5`（シナリオ ② + ③）**。SP-10 T-10-06 / T-10-07 の申し送り）（2026-09-17 に追加。SP-10 の完了確認） | SP-10（T-10-07。`tests/e2e/admin-demo.spec.ts`）/ **`F-053 AC-3` は本スプリントで green を確認** |
- **Phase 2 / 3 のシナリオ 6 件**（#11 / #12 / #14 / #19 の一部 / #21 / #22）は本スプリントの対象外。**ただし #19（AI 全停止）は Phase 1 の範囲で T-12-05 が扱う。**
- ⚠️ **#23 前半（AI 上限と HELD → `S-038` の残量表示 → 承認・送信の 409 / 422）はブラウザ経路では未実装のまま結合層（`tests/isolation/gate-hold-release.test.ts`）にある**（SP-09 T-09-11 ② の申し送り。ハーネスの env は 1 組で他の全シナリオがゲート PASS を前提にするため、上限到達を E2E で再現するには spec 単位で `AI_DAILY_COST_LIMIT_USD_DEFAULT` を変えた worker か、上限値を直接置くシームが要る）。🔴 **どう満たすかは `T-12-14` ⑤ で決め、決めた形を本表の #23 行に書く**（読み替えを固定しないまま「E2E」と書き続けない —— `docs/dev-plan.md` §8 の読み替えの前例 5 回と同じ扱い）。
- 🔴 **件数の内訳**: `docs/05` §17.3 の全 ~~**24**~~ ~~**25**~~ **26** シナリオ − 除外 **6** 件（#11 / #12 / #14 / #19 / #21 / #22）= ~~**18**~~ ~~**19**~~ **20 シナリオ**。上表の行数・本文の件数・§5 テスト計画の件数は**この 20 で一致していなければならない**（食い違うと完了確認モードが「どれが必須か」を機械的に判定できない）。**除外リストは #24 / #25 / #26 の追加後も変わらない**（#24 は Phase 1 の `F-064 AC-10`、#25 は Phase 1 の `F-008 AC-5` / `AC-6` / `F-019 AC-2` / `AC-5`、#26 は Phase 1 の `F-053 AC-2` / `AC-3` / `AC-6`）。
- 🔴 **読み替えの固定（2026-09-17。SP-10 の完了確認で決着した分。「E2E」と書かれたまま実体が結合 / render にあるものを、ここで名指しする）**: ①**`F-028`（非本番バナー）の「5 環境 × 主平面 / 管理平面 / モバイル」**（SP-10 T-10-05）→ 環境ごとの文言分岐は render テスト `apps/web/app/_components/environment-banner.render.test.tsx` / `apps/web/app/layout.render.test.tsx`（4 環境 + `production` で出ない）、**主平面 / 管理平面のブラウザ経路は `tests/e2e/isolation.spec.ts`（`expectEnvironmentBannerPinned`。`development` のみ）、モバイルは `tests/e2e/home.mobile.spec.ts` の 1 ケース（2026-09-17 追加）**。ハーネスの `APP_ENV` は `development` 固定であり、他環境をブラウザで通す手段は作らない（#26 と同じ理由）②**#17 / #24 の削除側は結合 `tests/isolation/tenant-purge.test.ts`**（上表のとおり。`docs/05` §17.3 #17 / #24 の注記）。🔴 **`T-12-04` の「`development` / `demo` で全分類の送信を実行」と本表は重複させない**（`docs/05` §17.4 の規律）。
- 🔴 **`globalSetup` と直列化の規律を守る**（`docs/05` §17.6）: 分離検証のシナリオ（#1 / #2 / #5 / #6 / #15 / #16）は **`workers: 1`**。テナントはテストごとに独立。時刻は `now` を引数で渡し**システム時刻を動かさない**。
- **完了の判定**: 上表がすべて green。**実行ログを保存し、Phase 1 完了確認の証跡にする。**

### T-12-04 環境分離の総合検証（M）

- **実装**: `docs/05` §17.4 / `docs/02` 章 7.6 NFR-ENV-1〜10。
- 🔴 **2026-09-10 の [Issue #20](https://github.com/Festal-KM/SES-Platform/issues/20) の回答（「Production と demo だけでよい」）により、Phase 1 で立てる環境は `development` + `demo` + `production` の 3 つになった。`sandbox` の検証は Phase 2（`T-13-11`）へ移す。**
  | 環境 | 検証 | Phase |
  |---|---|---|
  | `development` / `demo` | 🔴 **全分類の送信を実行し、外部エンドポイントへの発信が 0 件**（ネットワーク遮断 + `callCount()` の二重） | ✅ **Phase 1** |
  | `production` の起動検証 | 🔴 **モック実装が選ばれたら起動失敗 / 非本番に本番キーがあれば起動失敗** | ✅ **Phase 1** |
  | 🔴 **`production` の送信経路** | 🔴 **`production` から取引先へ届く送信が、送信ドメイン検証済みのテナントでのみ実行される**（NFR-ENV-10）。⚠️ **実 SES の送信を伴うため、E-1（本番アクセス承認）が前提である** —— **未承認ならこの行は満たせず、リリースできない**（`docs/dev-plan.md` §6.4 R-02 / CP-2） | ✅ **Phase 1** |
  | ~~`sandbox` ①~~ 分類 3 / 4 の送信で外部発信 0 件 | 🔴 **Phase 2 へ**（`SANDBOX` の環境もテナントも存在しないため実行できない） | **`T-13-11`** |
  | ~~`sandbox` ②~~ 分類 1 / 分類外が実際に送信され、宛先がホスト所属利用者または `PlatformUser` のみ | 同上 | **`T-13-11`** |
  | ~~`sandbox` ③~~ 分類 2（取引先招待）で外部発信 0 件。招待リンク経由で `PARTNER_ADMIN` が受諾・ログイン | 同上 | **`T-13-11`** |
- 🔴 **宛先分類そのもの（`recipientClass` / [Issue #9](https://github.com/Festal-KM/SES-Platform/issues/9) / [#10](https://github.com/Festal-KM/SES-Platform/issues/10)）は SP-04 で実装済みであり、Phase 1 のうちに `development` / `demo` で検証される。** **`sandbox` へ移るのは「その環境で通す」ことだけであり、コードは残っている**（`docs/sprints/SP-10-usage-env-sandbox.md` §4 T-10-08 の線引き表）。
- 🔴 **NFR-ENV-10**（送信ドメイン検証）: 未検証テナントで取引先へ届く送信が実行されず、共通ドメインへフォールバックしない。
- **完了の判定**: 上表の **Phase 1 の 3 行**がすべて green。🔴 **`sandbox` の 3 行を「未実施」として残さない** —— **到達不能な条件を完了判定に置くとスプリントが永久に閉じない**（`docs/dev-plan.md` §8 の 2026-09-01「完了判定の限定」の行）。**送り先（`T-13-11`）を明記したうえで外す。**

### T-12-05 AI 全停止時の縮退（M）

- **実装**: `docs/05` §17.3 #19 / `docs/02` 章 8.7。
- **検証**: 🔴 **AI が全停止（API 障害 / キー無効 / 上限到達）でも、`F-021` の承認と `F-022` の送信が成立し、ゲートは安全側に止まる。**
  - LLM の失敗・タイムアウト・スキーマ違反 → **PII 層 / 商流層は判定不能 = FAIL**（`GATE_FAILED`。PASS へフォールバックしない）。
  - AI の日次コスト上限 → **`GATE_RUNNING` のまま HELD**（`GATE_FAILED` にしない）。上限解除で自動復帰。
  - **既にゲートを通過して `APPROVED` になっている提案は、AI 停止中でも送信できる。**
  - **整合層の機械的照合は AI 停止中も動作する。**
- **Phase 2 の `F-043`（満了アラート）は本スプリントの範囲外**（#19 の該当部分は SP-16 で追加）。
- **完了の判定**: 上記 4 点が green。

### T-12-06 🔴 R-1 リリース条件（匿名 5 項目の丸め方の承認）（S）

- **何を行うか**: `CLAUDE.md` §5 の 🔴「**再識別リスクの評価（§9-13）は Phase 1 のリリース条件である**」を満たす。
- 🔴 **現状（2026-09-10 に 2 件が決着した。本タスクの性質が「承認を取る」から「承認を記録し、実データで裏づける」に変わった）**:
  | # | 論点 | 決着 |
  |---|---|---|
  | 1 | **承認者は誰か**（`PM-Q-1` / [Issue #18](https://github.com/Festal-KM/SES-Platform/issues/18)） | ✅ **回答 ①: 事業責任者が単独で承認する**（既定として置いていた ②「事業責任者 + 法務 / 個人情報保護担当の 2 名」から変更） |
  | 2 | **丸め粒度そのもの**（[Issue #5](https://github.com/Festal-KM/SES-Platform/issues/5)） | ✅ **回答「OK です」= `docs/03` §4.13.1（= `docs/02` A-04）の粒度で確定。** **経験年数 5 段階 / 単価 10 万円刻み・100 万円以上打ち止め / 稼働可能時期 5 段階 / 勤務地は都道府県 + リモート 3 値 / スキルは辞書名の上位 8 件。k-匿名性の件数閾値は Phase 1 では入れない** |
- 🔴 **承認者が単独になったぶん、判断の根拠を残す運用を必ず伴わせる**（2026-09-10 に本計画から提案し、[Issue #18](https://github.com/Festal-KM/SES-Platform/issues/18) に記録した）。**第三者の個人情報の再識別リスクを 1 人で判断する以上、「誰が・何を見て・何を許容したか」が後から辿れなければ説明責任を果たせない。**
- **手順**:
  1. SP-08 の実装（丸め関数と `packages/config/src/anonymize.ts` の定数）と、`T-12-01` の `seed:perf` による実データ相当の母集団で、**5 項目の組み合わせの一意率を算出して提示する**（`docs/03` §4.13.2-4 の監視指標）。
  2. 🔴 **承認の記録に次の 3 点を必ず書き残す**（**これが本タスクの成果物である**）: ①**母集団規模** —— 匿名共有が有効なエンジニアの件数、パートナー社数、テナントあたりの分布（**母集団が小さいほど特定されやすい**）②**丸め粒度** —— 実際に適用した 5 項目の刻み（`docs/03` §4.13.1 の値をそのまま転記する。**「§4.13.1 のとおり」で済ませない** —— 後から §4.13.1 が変わると、何を承認したのか分からなくなる）③**残存リスクの見立て** —— 一意率の実測値、**特定されうる条件**（例: 特定県 × 希少スキル × 単価上限帯）、**それを許容する理由**、**再評価のトリガ**（例: 匿名共有の件数が N を割ったら k-匿名性の閾値を入れる）。
  3. 承認結果を [Issue #5](https://github.com/Festal-KM/SES-Platform/issues/5) にコメントし、`docs/03` §4.13.1 / `docs/02` A-04 / `docs/05` TBD-2 を**確定形に更新する**（`CLAUDE.md` §8.7。上流から順に。**「暫定。Issue #5 で確認中」の注記を外す** —— 4 箇所以上に付いているので**一部だけ外さない**）。
  4. 承認の事実（日付・承認者・上記 3 点の記録先）を `docs/dev-plan.md` §8 の意思決定ログに追記する。
- 🔴 **人間の承認が無ければ Phase 1 を完了としない**（完了確認モードで `PHASE_INCOMPLETE`）。**「実装は終わっているから通す」を認めない。** ⚠️ **回答「OK です」を得た今も本タスクは消えない** —— **残っているのは「実データで裏づけ、根拠を記録すること」である**（承認は得たが、承認の根拠はまだ紙に無い）。
- **完了の判定**: ①承認の記録（Issue #5 のコメント + `docs/dev-plan.md` §8）が存在する ②🔴 **手順 2 の 3 点（母集団規模 / 丸め粒度 / 残存リスクの見立て）がすべて書かれている** ③上流 3 文書から「暫定」の注記が外れている。

### T-12-07 席単価・決済手数料率の再提起（S）

- **何を行うか**（`docs/03` `pm` 申し送り 14 / `Q-T-3` / `Q-T-6`）: 🔴 **席単価だけは Phase 1 のうちに人間へ再提起する。** 料金モデルが決まらないと `F-062`（課金）の Stripe `Price` 設計が始められず、**Phase 3 で待ちが発生する。**
- 論点: ①席単価（¥3,000 / ¥1,000 / ¥5,000 の 3 案。`docs/03` §7.5-3）②🔴 **取引先（`PARTNER_ADMIN` / `PARTNER_SALES`）の席を課金対象に含めるか**（`docs/01` `Q-20`。**含めると取引先を招くほど顧客のコストが上がり、母集団の拡大という中核価値と料金体系が逆を向く**）③決済手数料率（`Q-T-6`）。
- **件数クォータの値は Phase 2 の実測後でよい**（SP-14 の **T-14-08**（AI 実測原価の測定と上流ドキュメントの更新）と同時。`docs/03` `pm` 申し送り 7 / 14）。
- **完了の判定**: Issue #12 に再掲され、回答期限（SP-17 着手前）が設定されていること。

### T-12-08 固定費の実額確定と `docs/03` の更新（S）

- **何を行うか**（`docs/03` `pm` 申し送り 8 / `U-9` / `U-10`）: AWS Pricing Calculator で固定費の実額を確定させ、**`docs/03` §7.4 の $400〜$900 / 月 のレンジを実額に置き換える**（`CLAUDE.md` §8.7。ドキュメントを直す）。
- あわせて `T-A-06`（S3 単価）/ `T-A-07`（固定費の内訳）を実測値に更新する。`packages/config/src/pricing.ts` の設定値を反映する。
- **完了の判定**: `docs/03` §7.4 と `packages/config/src/pricing.ts` が更新されていること。

### T-12-09 🔴 本番 / `sandbox` の AWS アカウント分離（E-2）+ GuardDuty スキャン所要時間の実測（E-13）（M・非コードタスク中心）

- 🔴 **2026-09-10 の読み替え（[Issue #20](https://github.com/Festal-KM/SES-Platform/issues/20)）**: **`sandbox` が Phase 1 から外れたため、本タスクで作るのは「本番アカウント」と「開発（非本番）アカウント」の分離である。** 🔴 **目的は変わらない** —— **`CLAUDE.md` §11.1 の「本番以外の環境から実在の取引先へ送信が飛ぶ」事故を、実装だけでなく構成でも不可能にする**こと。**非本番アカウントの認証情報から本番の S3 / RDS / SES へ到達できないことを確認する**（下記 3）。**`sandbox` 用アカウントの作成は `T-13-11`（Phase 2）で行い、そのときも同じ規律を適用する**（`docs/03` §3.2.8 は「本番と `sandbox` を別アカウントに」と定めており、**この要求は消えていない。時期が後ろになっただけである**）。
- **背景**: T-01-09（2026-09-02）で整備したのは**開発用の単一 AWS アカウント**（IAM `claude-cli` / `ap-northeast-1` / 予算アラート $50 / 月）だけである。🔴 **`docs/03` §3.2.8 / `docs/03` `program-design` 申し送り 6 が求める「本番と `sandbox` を別アカウントにする」は未実施**（`docs/dev-plan.md` §5 E-2 / §8 の 2026-09-03 の行）。**Phase 1 のリリース前に解消する。**
- **何を行うか**:
  1. AWS Organizations で**本番 / `sandbox` / 開発**を分ける。🔴 **少なくとも本番と `sandbox` は別アカウントにする**（`CLAUDE.md` §11.1 の「本番以外の環境から実在の取引先へ送信が飛ぶ」事故を、実装だけでなく**構成でも**不可能にするため）。
  2. S3 は各アカウントに **1 バケット + テナント別プレフィックス**で用意する（GuardDuty Malware Protection for S3 の保護バケット上限 25。`docs/03` `program-design` 申し送り 16）。**バージョニングを有効化する**（`FileScanResult` の重複排除キーが版 ID を要求する。`docs/05` §14.1）。
  2b. 🔴 **バケットに CORS を設定する（`PutBucketCors`）**（T-05-06 / T-05-10 の e2e-tester 所見。`docs/05` §14.2 / `docs/03` §4.18.1）。スキルシートは**ブラウザから直接 PUT** するため、`Content-Type` を伴う非単純リクエストとしてプリフライトを受ける。**S3 は既定で CORS を一切許可しない**ので、未設定だと `S-008` の画面からのアップロードだけが必ず失敗する（サーバ側のテストは全て green のまま）。`AllowedMethod: PUT` / `AllowedHeader: content-type, content-length`（SSE-KMS を使う環境では `x-amz-server-side-encryption*` も）/ `AllowedOrigin` は**その環境のアプリ origin だけ**（🔴 `*` にしない）。⚠️ **`development`（MinIO）は既定で全 origin を許可するため設定が無い** —— **ローカルで動くことがこの設定を省いてよい理由にはならない**（この差自体が見落としの温床であり、だからチェックリストに項目として置く）。
  3. 🔴 **非本番アカウントの認証情報から本番の S3 / RDS / SES へ到達する権限を与えない。** IAM ロール・キーをアカウント間で共有しない。
  4. アカウントごとに予算アラートを設定する（開発用の $50 / 月 と同等以上の粒度）。
  5. 接続先・リージョン・バケット名は `packages/config` の環境変数から解決する。🔴 **アカウント ID やバケット名を実装にハードコードしない。**
  6. 🔴 **GuardDuty のスキャン所要時間を実測する（E-13 / [Issue #37](https://github.com/Festal-KM/SES-Platform/issues/37)）。** 上記 2 で用意した保護バケットへ代表的なスキルシート（xlsx / docx / pdf の各サイズ）を投入し、**アップロード完了から `FileScanResult` の受信までの実時間**を記録する。🔴 **`docs/02` 章 7.1 の「2 分以内」を満たせない場合は、目標値の見直しを人間に提起する**（`SCAN_STALL_ALERT_MINUTES` の既定 10 を変えるかどうかとは別の判断である）。**実測値は `docs/dev-plan.md` §8 の意思決定ログに追記し、§5 E-13 の行と §9 の `U-7` / TBD-7 の行を「実測済み」に更新する。** 🔴 **本項が SP-05 ではなくここにあるのは、実測に GuardDuty Malware Protection for S3 を有効化した実 AWS アカウントと保護バケットが要るからである**（T-05-05 の設計・実装は所要時間に依存しておらず、滞留の判定は `SCAN_STALL_ALERT_MINUTES` の 1 設定値だけで成立している。`docs/sprints/SP-05-engineer-ledger.md` §4 T-05-05 / `docs/dev-plan.md` §5 E-13）。
- 🔴 **T-12-04（環境分離の総合検証）の前提である。** T-12-04 は実装（DI）と `callCount()` とネットワーク遮断で「`production` 以外から実在の宛先へ 0 通」を担保するが、**本番キーの混入（NFR-ENV-4）はアカウントが分かれていて初めて構成として塞がる。** 順序は **T-12-09 → T-12-04** とする。
- 🔴 **本タスクはコードを伴わないが、スプリントのタスクとして扱う**（T-01-09 と同じ扱い）。実施日・アカウント ID の対応・予算アラートの設定先を `docs/dev-plan.md` §8 の意思決定ログに追記し、§5 E-2 の行を「分離済み」に更新する。
- **完了の判定**: ①本番と `sandbox` が別アカウントであること（アカウント ID の対応表が記録されている）②非本番の認証情報で本番リソースへアクセスすると拒否されることの確認記録 ③`docs/dev-plan.md` §5 E-2 と §8 が更新されていること ④🔴 **GuardDuty のスキャン所要時間が実測され、`docs/dev-plan.md` §8 / §5 E-13 / §9 の `U-7` に記録されていること（E-13 / [Issue #37](https://github.com/Festal-KM/SES-Platform/issues/37)）。`docs/02` 章 7.1 の 2 分以内を満たせない場合は、目標値の見直しが人間に提起されていること。** ⑤🔴 **Amazon SNS の SMS サンドボックス解除 + 月次上限引き上げの申請を、①で作った本番 AWS アカウント × `ap-northeast-1` に対してサポートケース 1 件で提出済みであり、提出日（と応答日・付与額）が `docs/dev-plan.md` §5 E-17 / §8 に記録されていること**（2026-09-17 追加。SP-11 の完了確認 NG-1 = [Issue #62](https://github.com/Festal-KM/SES-Platform/issues/62)〔人間の作業〕。`docs/03` §3.10.4 / `docs/03` `pm` 申し送り 16-①。**申請先のアカウントが本タスクで初めて存在するため、`T-11-10` ②③ の実務はここに合流する** —— 付与後にコンソールで上限値を書き換えないと $1 のままである点まで含む。申請額の既定は $200 = [#65](https://github.com/Festal-KM/SES-Platform/issues/65)）。

### T-12-10 🔴 公開後の公開欄の編集と再検査の決着（[Issue #42](https://github.com/Festal-KM/SES-Platform/issues/42)）（S〜M）

- **何を行うか**: 🔴 **公開済みの案件の「公開欄」（案件名 / 公開文 / 要件のフリーテキストの 3 欄）を編集したときに、品質ゲートを再検査するかどうかを決着させ、決定に応じて実装・検証まで行う。**
- **背景**（SP-07 の T-07-09 で判明。`docs/sprints/SP-07-ai-layer-gate.md` §4 T-07-09 / `docs/dev-plan.md` §9）: 公開の**時点**では `F-014 AC-3` により商流層 FAIL なら公開できない。しかし**公開後の編集は現在ゲートを通らない**ため、🔴 **取引先に見えている内容に、後からエンド企業名・内部単価・他社名が入る経路が残っている。** `PROJECT_PUBLISH` の `contentHash` には公開先と取引先の社名まで含めてあるので、**「編集でハッシュが変わる」ことは検知できる**（確定済みのゲート結果はキャッシュとして再利用されない）—— **足りないのは、変わったときに何をするかの決定である。**
- **選択肢**:
  | # | 案 | 得失 |
  |---|---|---|
  | ① | **現状維持**（編集しても公開は解除されない） | 実装が増えない。🔴 **上記の穴が残る**（`F-014 AC-3` が「公開の瞬間だけの検査」になる） |
  | ② | 🔴 **再検査し、FAIL なら公開を解除する**（**推奨**） | 穴が塞がる。⚠️ **編集のたびにゲートが走るため AI 原価が増え、公開中の案件が編集の途中で一時的に見えなくなる**（利用者への説明が要る） |
  | ③ | 再検査するが、FAIL でも公開は維持し警告のみ出す | 🔴 **採らない。** **ゲート FAIL を無視して外部に出せる状態そのもの**であり、`BR-18` / `CLAUDE.md` §3.3 に反する |
- ✅ **決着した（2026-09-10。人間の回答「２で」= 上表の②）。** 🔴 **既定として置いていた ①（現状維持）から変更された。** **編集で再検査し、FAIL なら公開を解除する。** **本タスクは「判断を仰ぐ」から「②を実装して検証する」に変わった。**
- 🔴 **対象は 3 欄に限る**: **`projects.name`（案件名）/ 公開用サマリ（公開文）/ `project_requirements` のフリーテキスト**。⚠️ **`T-07-09` で `PROJECT_PUBLISH` の `contentHash` に公開先と取引先の社名まで含めたため、「検査対象の欄」と「ハッシュに含める値」は同じではない** —— **ハッシュが変わる = 再検査が要る、とは限らない**（公開先を 1 社増やしただけで本文が同じなら、商流層の判定は変わらない）。**再検査の判定は上記 3 欄の変更で行い、公開先の増減は別に扱う**（増えた公開先に対する検査は公開の時点で `F-014 AC-3` が行う）。
- **手順**:
  1. 🔴 **上流から順に直す**（`CLAUDE.md` §8.7）—— **`docs/02` `F-014` の AC（公開後の編集で再検査し、FAIL なら公開を解除する）→ `docs/04` §S-011 / §S-013 の文言（下記②）→ `docs/05` §11.11（`settleProjectPublish` と再検査の接続）→ 実装とテスト。**
  2. 🔴 **公開範囲の行を作る / 落とすのは `settleProjectPublish` の 1 か所のままにする**（T-07-09 で 1 か所に閉じてある。**2 か所目を作ると、どちらか一方だけがゲートを見ない経路になる**）。
  3. 🔴 **AI 原価の増加を抑える**: 3 欄が**実際に変わったときだけ**再検査する（`contentHash` の比較。**保存のたびに走らせない**）。**上限到達時は `HELD_AI_COST_LIMIT` で保留し、`GATE_FAILED`（＝公開解除）に倒さない**（`docs/dev-plan.md` §6.4 R-04。**原価で止まっただけの案件を、取引先から消してはならない**）。
- 🔴 **受け入れ基準（②を採ったことによる副作用を含む）**:
  | # | 基準 | 補足 |
  |---|---|---|
  | ① | **3 欄のいずれかを編集すると再検査が走り、商流層 FAIL なら `ProjectVisibility` の行が落ちて、パートナー文脈から当該案件が見えなくなる** | 結合テスト（`tests/isolation/project-publish-gate.test.ts` に追加）。🔴 **「見えない = 存在しない」を守る**（404。件数・並び順にも痕跡を残さない。`F-004 AC-3`） |
  | ② | 🔴 **利用者に理由が明示されること。** ホスト側（`S-011` / `S-013`）に「**検査で問題が見つかったため公開を解除しました**」と**どの欄が原因か**を出し、`F-020` の指摘へ導線をつなぐ。🔴 **ゲート FAIL を「無視して公開」する導線は作らない**（`CLAUDE.md` §3.3。直せるのは元データだけ） | ⚠️ **これが②の最大の副作用である** —— **取引先の画面から案件が消える。** 理由が出ないと、ホストは「なぜ問い合わせが止まったのか」を知る手段が無い |
  | ③ | 🔴 **取引先側には理由を出さない。** 取引先から見えるのは「公開されていない」状態だけであり、**`S-011` の既存挙動（公開解除された案件を開いたときの文言。HTTP は 404 のまま）に合流させる**（`docs/04` §S-011）。**ホスト内部の検査結果を取引先に見せない** | `CLAUDE.md` §3.1 / `BR-67` と同じ考え方 |
  | ④ | **解除は監査ログに残る**（`project.visibility_change` の before / after。`F-014 AC-5`）。🔴 **自動解除であることが記録から分かる**（人の操作と区別できる） | `T-11-09` で `summary` が画面から読めるようになる |
  | ⑤ | **再検査が PASS なら公開はそのまま維持される**（無用な解除・再公開の往復を起こさない） | 結合テスト |
  | ⑥ | 🔴 **上限到達（`HELD_AI_COST_LIMIT`）では公開を解除しない。** 保留のまま維持し、解除後に再検査する | **原価の都合で取引先の画面から案件が消えるのは、事故と区別がつかない** |
- **完了の判定**: ①決定（②の採用）が Issue #42 のコメントと `docs/dev-plan.md` §8 に記録されている ②上流 3 文書（`docs/02` / `docs/04` / `docs/05`）が先に更新されている ③上表①〜⑥の結合テストが green ④`tests/isolation/project-publish-gate.test.ts` の既存 11 本が引き続き green。

### T-12-11 🔴 第 1 回リリースの準備（L・非コードタスク中心）

- **背景**: 2026-09-10 に人間が「**Phase 1、Phase 2、Phase 3 のそれぞれでリリースする**」と決定した（[Issue #46](https://github.com/Festal-KM/SES-Platform/issues/46)）。**第 1 回は Phase 1 完了（本スプリントの後）である。** 🔴 **3 回のうち本番環境をゼロから立てるのは第 1 回だけであり、本タスクが最も重い。** 第 2 回（`T-16-11`）/ 第 3 回（`T-20-11`）は差分リリースであり、**本タスクの受け入れ基準を複製しない**（`docs/dev-plan.md` §2.2 / `PM-A-10`）。
- 🔴 **本タスクの一部は「リリースに何が要るかの洗い出し」そのものである。** 下記 ①〜⑨ は**最低限**であり、網羅の責任は本タスクにある。**洗い出しの結果として項目が増えたら、それも受け入れ基準に加えたうえで `docs/dev-plan.md` §2.2 / §8 に記録する**（`CLAUDE.md` §8.7。**チェックリストが担当者の頭の中にしか無い状態を作らない**）。
- **受け入れ基準**:
  1. 🔴 **本番環境の構築が済んでいること。ただし本タスクで作業を重複させない** —— **AWS アカウント分離（本番 / `sandbox`）と GuardDuty の実測は `T-12-09` が持つ**ので、本タスクは「**`T-12-09` が完了し、記録（アカウント ID の対応表・拒否の確認記録）が `docs/dev-plan.md` §5 E-2 / §8 にあること**」を確認する。**同様に、SES の本番アクセス承認（E-1）と送信ドメイン検証（`T-04-05` / `T-04-06`）も「済んでいることの確認」として参照する。** 🔴 **E-1 が未承認のままではリリースしない** —— 取引先へのメールが本番で 1 通も出せず、`F-022` が成立しない（CP-2。`docs/dev-plan.md` §4.2 / §6.4 R-02）。
  2. 🔴 **`production` の環境変数一式が揃い、`packages/config` の Zod スキーマによる起動時検証を通ること。** 併せて **`APP_ENV=production` でモック実装が選ばれたら起動が失敗すること**を実環境で 1 回確認する（`CLAUDE.md` §11.1。**「未設定ならモックにフォールバック」は成功したように見えて送信されていないという最悪の壊れ方を生む**）。🔴 **Anthropic API キー（E-3）が無いと `production` はワーカーの起動自体に失敗する**（`packages/ai` は fail-closed）。**キーの取得はユーザー作業であり、リリースの前提である**（[Issue #19](https://github.com/Festal-KM/SES-Platform/issues/19) / `Q-07-1`）。
  3. 🔴 **障害時の運用手順が文書として存在すること。** **「誰が何を見るか」を書く**: ①Sentry のアラート宛先 ②`A-005`（運用監視）で見る項目 —— **失敗ジョブ / `SUBMITTING` の滞留 / `SUBMIT_FAILED` の未対応 / ウイルススキャン失敗 / 送信基盤の上限到達（`HELD_PROVIDER_QUOTA`。項目 13〜15）/ ゲート FAIL 率の異常** ③🔴 **`SUBMIT_FAILED` からの復帰は人間の明示操作のみである**こと（自動リトライしない。`CLAUDE.md` §4.2）④`gate.run` の失敗ジョブの再実行手順（`docs/05` §9.10 の 5 手順。入口は API #39 のみ）⑤DB のバックアップとリストアの手順 ⑥連絡経路（一次受け・エスカレーション先）。**置き場所は `docs/` 配下とし、リンクを本タスクの完了記録に残す。**
  4. 🔴 **課金は手動請求で開始する**（Stripe は SP-20）。**請求金額の算出根拠が管理平面（SP-11）の画面から取れること**を実データで確認する: **席数（`A-004` / `F-057`）と AI 利用の件数・金額（`A-003` / `F-057`）がテナント × 月で読め、締め日時点の値を後から再現できること**。🔴 **利用者に見せる残量は件数のみ、金額は運営者の内部指標である**という区別を崩さない（`CLAUDE.md` §2 / [Issue #12](https://github.com/Festal-KM/SES-Platform/issues/12)）。**手動請求の実務手順（誰がいつ画面から値を取り、どの様式で請求するか）も ③ の文書に含める。**
  5. 🔴 **契約書はメール添付で運用することを、利用者向けの制約として明記すること。** `Contract` の状態機械・テンプレート差し込み・契約書の品質ゲート・冪等送付は **SP-17**（Phase 3）であり、**リリース時点では契約の進行をプラットフォーム上で追跡しない。** ⚠️ **「契約書もゲートを通る」は `CLAUDE.md` §3.3 の要求だが、その対象物がまだプロダクト内に無い** —— **プラットフォーム外でやり取りされる契約書にゲートは効かない**ことを明示する（運用でカバーする範囲として合意する）。
  6. 🔴 **利用者向けの制約説明（`docs/dev-plan.md` §2.2 の RL-1〜RL-5）が用意されていること。** 特に **RL-3（⑥ → ① の還流がまだ無い）** は `CLAUDE.md` §1.3 の中核が未提供であることを意味するため、**「①〜④ の一本化までである」「満了管理は当面 Excel に残す」ことをリリース時に合意する**（§6.4 R-12）。
  7. 🔴 **`SP-21`（UI 基盤 = Tailwind CSS + shadcn/ui）が完了していること。** 素の CSS のまま顧客が触る画面を出さない（[Issue #43](https://github.com/Festal-KM/SES-Platform/issues/43) / `docs/dev-plan.md` §3.2.1 / §6.4 R-13）。<br>✅ **完了した（2026-09-11。`T-21-01`〜`T-21-07` の 7 / 7）。参照先は `docs/dev-plan.md` §8 の 2026-09-11 の 3 行 と `docs/sprints/SP-21-ui-foundation.md` §8（§8.2 に §7 の完了判定 13 項目の判定表、§8.7 に検証環境の但し書き）である。** 🔴 **確認する値**: `.ses-` の className **0 件** / `apps/web/app/globals.css` が**存在しない**（スタイルシートは `tailwind.css` の 1 本）/ testid の削除・改名 **0 件** / `t(` のファイル単位の減少 **0 件** / CI run **`34553246355`** green（E2E 35 passed。**ビルドを挟んだ実行**）。⚠️ **本タスクでは再実装・再検証をしない** —— **上記の記録が在ることの確認**である（`T-12-09` を参照する ① と同じ扱い）。🔴 **ただし SP-21 の申し送り 2 件（`T-08-10` / `T-08-11`）が消化済みであることは併せて確認する**（`docs/sprints/SP-08-anonymous-share.md` §7 の 9 / 10）。
  8. ⚠️ 🔴 **`staging`（`T-13-09`）も `sandbox`（`T-13-11`）も第 1 回リリースより後になることの扱いを明記すること**（2026-09-10 の [Issue #20](https://github.com/Festal-KM/SES-Platform/issues/20) の回答「Production と demo だけでよい」による。`PM-Q-3`）。**通常と逆の順序である** —— **本番相当の検証を `demo` と `T-12-04`（環境分離の総合検証）と `T-12-03`（E2E）でどこまで代替したかを書き残す。** 🔴 **代替できていない範囲を正直に列挙すること**: ①**本番と同じ外部エンドポイント構成での事前検証が無い**（`staging` が無いため、`production` が初めての実接続になる）②**実データを入れた試用の実績が無い**（`sandbox` が無いため、`demo` の合成データまでしか通っていない）。🔴 **したがって第 1 回リリース直後の監視（`A-005`）を厚くする** —— 失敗ジョブ・`SUBMITTING` の滞留・送信保留の理由別内訳を、**リリース後しばらくは日次で人が見る**手順を ③ の文書に書く。
  8b. 🔴 **「見込み客が自分の実データで試す」導線が第 1 回リリース時点で無いことを、営業・事業側と合意すること**（`docs/dev-plan.md` §2.2 `RL-11`）。⚠️ **`demo`（合成データ・実演専用）に見込み客の実データを入れて代替してはならない**（`CLAUDE.md` §11.1「`demo` と `sandbox` を兼ねない」。**実演中に実在の取引先名が画面に出る事故につながる**）。**解消は Phase 2（`T-13-11` / `T-10-08`）である。**
  9. **リリース判定を `MODE: REVIEW` / `TARGET: Phase 1` で行い、`## PHASE_COMPLETE` を得たことを記録すること。** 🔴 **別のリリース手順を作らない**（手順が 2 つあると片方だけ通してリリースする経路ができる。`docs/dev-plan.md` §7）。
- 🔴 **本タスクはコードを伴わない項目が中心だが、スプリントのタスクとして扱う**（`T-01-09` / `T-12-09` と同じ扱い）。**実施日・確認結果・作成した文書のパスを `docs/dev-plan.md` §8 の意思決定ログに追記する。**
- **完了の判定**: ①〜⑨ のすべてに証跡（記録・文書のパス・確認日）があること。🔴 **1 つでも欠けたらリリースしない。**

### T-12-12 🔴 メール / ストレージのクォータ上書きの執行点配線（T-11-02 NG-1 の後続）（M）

- **背景**: `T-11-02`（利用量・クォータ管理と `A-004`）の code-reviewer レビュー（NG-1。2026-09-16）で、`tenant_quota_overrides` の上書きが `EMAIL_COUNT` / `STORAGE_BYTES` を含む 6 計測で実装され、**表示（`A-004`）と `usage.limit-check` の判定にだけ効き、実際の執行点は既定値を読んでいた**ことが発覚した（「停止と表示されているのに送信される」「上限を上げたのに保留される」事故になる）。応急処置として `QUOTA_OVERRIDE_METRICS` を **AI の月次件数 4 単位のみ**に絞り、`EMAIL_COUNT` / `STORAGE_BYTES` を上書きの対象から外した（NG-1 の修正 (b)）。**本タスクは、その 2 計測を安全に再導入するための配線である。**
- **実装**: 次の 3 か所を `resolveTenantQuotas`（`packages/db/src/quota-overrides.ts`）から読むように配線する。
  1. `apps/worker/src/jobs/email-send.ts` の `decideEmailRate` / `reserveEmailDailyQuota` — テナントの日次メール上限を既定値ではなく `resolveTenantQuotas(...).emailDailyLimit` から取る。
  2. `apps/worker/src/jobs/send-hold-release.ts` — `HELD_DOMAIN_UNVERIFIED` 等の復帰判定でメール上限を再評価する箇所を同様に配線する。
  3. `apps/web/lib/skill-sheets/service.ts` の `issueSkillSheetUploadUrl`（`decideStorageUpload`）— ストレージ上限を `resolveTenantQuotas(...).storageLimitBytes` から取る。
- 🔴 **設計判断点（着手前に決める）**:
  - **パートナー文脈での読み取り**: `resolveTenantQuotas` は `HostTenantCtx` を要求するが、`issueSkillSheetUploadUrl` は `PARTNER_*` ロールからも呼ばれる（パートナーが自社のスキルシートをアップロードする）。`HostTenantCtx` 要求のままでは型が通らない。**`usage_counters_storage_select`（migration 20260907000000 §2。列の値でポリシーを絞る）と同じ判断**で、`tenant_quota_overrides` の SELECT ポリシーをパートナー文脈にも開くか、パートナー文脈では常に既定値に倒すかを決める。
  - **列 GRANT の絞り込み**: パートナー文脈への読み取りを開く場合、`reason`（運営者の自由記述）と `set_by_platform_user_id`（運営者の識別子）は業務ロールに見せない列である。列レベル GRANT で `metric` / `limit` / `effective_from` / `created_at` に絞る。
  - **メールの分次上限（`emailMinuteLimit`）は対象外のまま**（`packages/config` の値。今回の 2 計測に含めない。§5.8.1 ⑧ の既存の記述どおり）。
- 完了後、`QUOTA_OVERRIDE_METRICS`（`packages/domain/src/quota/override.ts`）に `EMAIL_COUNT` / `STORAGE_BYTES` を戻し、migration の CHECK 制約・Zod スキーマ・`A-004` の UI（`admin.usage.quota.defaultFixed` の専用表示を通常表示に戻す）を合わせて更新する。
- **完了の判定**: 上限を上げた翌日にテナントの日次メール送信数 / ストレージ上限が実際に変わることを結合テストで証明する（`tests/isolation/admin-usage-quota.test.ts` に統合するか新設）。パートナー文脈からの読み取りが他テナントの `reason` / 運営者 ID を返さないことをテストで証明する。

### T-12-13 🔴 SP-09 からの申し送りの回収 ①（上流改訂・人間判断を要しない整備）（M）

- **背景**: SP-09 の完了確認（2026-09-17。`docs/sprints/SP-09-proposal-flow.md` §8 ①）で、T-09-09 ③ / T-09-10 ③ / T-09-11 ① の「SP-12（Phase 1 hardening）へ」の申し送りが本スプリントに転記されていなかった。本タスクは 11 項目のうち**上流（`docs/04` / `docs/05`）の改訂や人間判断を要しない 6 項目**を回収する。残る 5 項目は `T-12-14`。🔴 **原文は SP-09 から写したものであり、言い換えない**（出典の節を消さない。本表が「何を回収したか」の照合先になる）。
- **原文と、何をどう直すか**:

| # | 出典 | 原文（SP-09 より） | 何をどう直すか | 完了の判定 |
|---|---|---|---|---|
| ① | T-09-09 ③-① | 🔴 **`audit_logs.target_type` の表記の統一** —— `'Proposal'`（`apps/web/lib/proposals/{approval,gate,resend,service,submit,transition}.ts` のローカル定数 `PROPOSAL_TARGET_TYPE` と `packages/db/src/{proposal-approval,proposal-send}.ts`）と `'PROPOSAL'`（`apps/worker/src/jobs/gate-run.ts` の `GATE_RESULT` / `packages/db/seed/presets/isolation.ts` の一部）が混在している。書き込み側を `PROPOSAL_AUDIT_TARGET_TYPE`（`@ses/db`。T-09-09 で新設）に寄せ、既存行を `UPDATE audit_logs SET target_type = 'Proposal' WHERE target_type = 'PROPOSAL' AND action LIKE 'proposal.%'` で移行する（`S-041` に対象種別フィルタを足す前に）。 | 書き込み側 8 ファイルのローカル定数を削除し `@ses/db` の `PROPOSAL_AUDIT_TARGET_TYPE` を import する。`gate-run.ts` の**監査行**（`audit_logs`）と `seed/presets/isolation.ts` も同じ定数に寄せる。既存行の移行は **新規 migration**（原文の `UPDATE` 文をそのまま。`action` / `summary` / `actor` / 日時は触らない —— **表記の統一であり、監査ログの改変ではない**ことを migration のコメントに書く）。⚠️ **`review_gates.target_type` / `ai_usage.target_type` / `send_attempts.entity_type` の `'PROPOSAL'` は別の列・別の定数（`GateTargetType` / `PROPOSAL_SEND_ENTITY_TYPE`）であり対象外。混ぜない。** 静的テストを 1 本足す: **`audit_logs` の `targetType` に `'Proposal'` / `'PROPOSAL'` のリテラルを書く箇所が非テストソースに 0 件**（定数経由のみ）。 | 静的テスト green。migration 適用後の結合テストで `target_type = 'PROPOSAL' AND action LIKE 'proposal.%'` が **0 行**、かつ `tests/isolation/proposal-list-detail.test.ts` ほか既存の監査行の検証が `PROPOSAL_AUDIT_TARGET_TYPE` で `gate.run` 由来の行まで引けること。 |
| ② | T-09-10 ③-① | `docs/05` §6.5「#48 の実装の決着」の「取引先は … 5 本」の記述を 6 本に訂正する（`pm` の文書整備。実装は 6 本で一致している）。 | `program-design` が `docs/05` §6.5「#48 の実装の決着」の該当文を **6 本（実施 / 結果待ち + 4 状態からの辞退）** に訂正する。**実装は変えない。** | `docs/05` §6.5 に「5 本」の記述が残っていない（Grep で 0 件）。 |
| ③ | T-09-10 ③-② | `S-024` の日時入力は `datetime-local` の壁時計をそのまま `note` に書く（TZ を持たない記録）。面談調整の連絡（`F-041`。Phase 2 SP-15）で日時を構造化して送るときは、`ProposalEvent.note` の自由記述ではなく別の列 / 型で持つこと（`note` を正規表現で読まない）。 | 実装は変えない。**制約を 2 箇所に固定する**: `docs/05` §6.5「`S-024` の実装の決着（T-09-10）」に「`note` の日時は `datetime-local` の壁時計（TZ 無し）であり、`F-041` で日時を送るときは `note` を読まず別の列 / 型で持つ」を注記（`program-design`）+ `apps/web/lib/proposals/interview-note.ts` の冒頭コメントに同じ 1 行。🔴 **Phase 1 完了後の再計画で SP-15 の `F-041` タスクの着手条件として転記する**（本スプリントでは SP-15 を触らない。`docs/dev-plan.md` §8 の 2026-09-17 の行に持ち主を記した）。 | 注記が 2 箇所に存在する。 |
| ④ | T-09-10 ③-③ | `S-024` の「直近の履歴 3 件」は #46 の `events` 全件を読んで末尾 3 件を切り出している（提案 1 件の履歴は多くて数十行なので現状は問題ないが、履歴が肥大したら #46 に `limit` を足す）。 | **見送り（条件付き）。** 実装は変えない。`docs/05` §6.5「#45 / #46 / #47 と `S-019` / `S-023` の実装の決着（T-09-09）」に **再評価条件「提案 1 件の `proposal_events` が 200 行を超える実例が出たら #46 に `limit` を足す」** を注記する（`program-design`）。 | 注記が存在する。 |
| ⑤ | T-09-11 ①-① | #44（再送）の 202 後に遷移する `S-021` は `APPROVED` として描かれ、送信中のポーリング（#46）は「送信する」を押した直後（`SUBMIT_REQUESTED`）と `SUBMITTING` の間しか走らない。**再送の確定（`SUBMITTED`）を画面が拾わず、読み直すまで「送信する」が押せる状態に見える**（押しても同じ `attemptSeq` で 1 本に畳まれるので二重送信にはならないが、利用者には分からない）。`S-022` から遷移した直後もポーリングを走らせるか、遷移先を `S-023` にするかを決める（E2E は `reload()` で回避している。`proposal-cycle.spec.ts` シナリオ 3 #8 の注記）。 | **既定 = 遷移先は `S-021` のまま**（`docs/05` §6.5「#44 と `S-022` の実装の決着（T-09-08）」の「202 後は `S-021` へ」を変えない。**`docs/04` §S-022 は遷移先を定めておらず「状態バッジが `送信中` へ」とだけ書く**ため、上流改訂を伴わない側を採る）。`apps/web/app/(main)/proposals/[id]/approve/proposal-approval-screen.tsx` のポーリング条件を「`SUBMIT_REQUESTED` / `SUBMITTING` の間」から「**`APPROVED` で、かつ #46 の `sendAttempts` の末尾が未確定（`RESERVED`）または `sendHold` が非 null**」にも広げ、`SUBMITTED` / `SUBMIT_FAILED` への確定を #46 の差分で拾う。🔴 **確定後に「送信する」を描かない**（`SUBMITTED` = 完了の表示 / `SUBMIT_FAILED` = `S-022` への導線）。**押しても 1 本に畳まれる事実は変えないが、押せる表示そのものを出さない。** E2E `tests/e2e/proposal-cycle.spec.ts` シナリオ 3 #8 の `reload()` 回避を外し、ポーリングで `SUBMITTED` を拾う形に置き換える。 | render テスト（`APPROVED` + 未確定 `SendAttempt` でポーリングが走る / 確定後に `proposal-approval-submit` が無い）green。E2E #8 が `reload()` 無しで green。 |
| ⑥ | T-09-11 ①-② | `S-021` の `data-can-approve` は立場の表明で状態を含まない（`GATE_FAILED` でも `true`）。属性名から状態の可否と誤読しうるので、`data-can-approve-now` を足すか render テストに注記する。 | `proposal-approval-screen.tsx` に **`data-can-approve-now`**（= 立場 × 状態 `APPROVAL_PENDING` × プレビュー末尾の確認済み。**この瞬間に #41 を呼べるか**）を足す。`data-can-approve` は立場の表明として**据え置く**（🔴 属性・testid の削除・改名 0 件 —— `tests/static/testid-inventory.test.ts` を壊さない）。render テストに両属性の意味の差を固定する。 | render テスト（`GATE_FAILED` で `data-can-approve="true"` かつ `data-can-approve-now="false"` / `APPROVAL_PENDING` + 末尾確認済みで両方 `true`）green。 |

- 🔴 **守ること**: ①は `audit_logs`（監査ログ表）への migration を含むため**境界領域として `code-reviewer` 1 回**（`CLAUDE.md` §8.3）。⑤⑥は UI のみ。②③④は文書整備であり、`docs/05` の改訂は **`program-design`** が行う（`CLAUDE.md` §8.7。**スプリントファイルとコードだけを直さない**）。
- **完了の判定**: 上表 ①〜⑥ の「完了の判定」列がすべて満たされている。🔴 **1 項目でも未消化なら、その項目を「見送り」と書かずに残す**（見送りは④の条件付き 1 件だけ）。

### T-12-14 🔴 SP-09 からの申し送りの回収 ②（上流改訂・人間判断を要するもの）（M）

- **背景**: `T-12-13` と同じ（SP-09 §8 ①）。本タスクは 11 項目のうち **`docs/04` / `docs/05` の改訂が先に要るもの、または人間判断（Issue）を要するもの 4 項目**（T-09-09 ③-②③⑤ / T-09-11 ①-③）と、**T-09-11 ②（SP-10 / SP-11 宛だが `T-12-03` の証跡リストに直結する E2E #23 前半）の 1 項目**を回収する（11 項目の残り 1 つ = T-09-09 ③-④〔`S-003` の要対応キュー〕は `T-12-15`）。🔴 **順序は上流から**（`CLAUDE.md` §8.7。**スプリントファイルとコードだけを直さない**）。🔴 **Issue の起票はオーケストレーターが行う**（`CLAUDE.md` §8.6。本タスクの `programmer` は `## BLOCKED` / 本表の「既定」で進め、回答を待って止まらない）。
- **原文と、何をどう直すか**:

| # | 出典 | 原文（SP-09 より） | 何をどう直すか（既定と選択肢） | 完了の判定 |
|---|---|---|---|---|
| ① | T-09-09 ③-② | **`S-019` の一括承認**（`docs/04` §S-019「一括操作（ホスト）」。デスクトップのみ）は置いていない —— `S-021` の「プレビューの末尾まで確認するまで承認できない」（`F-021 AC-4` / `BR-49`）と両立する設計（1 件ずつの判断材料の列挙）が先。 | 🔴 **人間判断（スコープ。`CLAUDE.md` §8.6）。Issue を起票する（`decision-needed`）。** 選択肢: **A = Phase 1 では置かず、`docs/04` §S-019 の「セクションごとのフェーズ」と「一括操作（ホスト）」を Phase 2 に改訂する**（`ui-design`）/ **B = Phase 1 で実装する**。ただし B は「1 件ずつの判断材料を列挙し、**各件のプレビュー末尾まで確認済みでなければ承認できない**」形でしか成立せず（`F-021 AC-4` / `BR-49` / `CLAUDE.md` §13.3「判断材料を見ずに承認できる導線を作らない」）、操作量は 1 件ずつの承認と同じになる。**既定 = A。** いずれでも `F-021 AC-6`（モバイルでは一括承認を既定にしない）は成立したまま。 | Issue が起票され、既定 A なら `docs/04` §S-019 が改訂されている。B なら実装 + `tests/e2e/proposal-cycle.spec.ts` シナリオ 5（モバイルで一括承認が既定でない）が引き続き green。 |
| ② | T-09-09 ③-③ | `S-023` の「ゲート結果の履歴」（`docs/04` §S-023 セクション 4「実行ごとの層別結果」）は**現在の結果（#40）だけ**を描いている。実行ごとの履歴は `review_gates` を `target_id` で複数行読む API（#40 の拡張）が要る。 | **`program-design` が `docs/05` §6.5 #40 を拡張してから実装する。** 既定の形: `GET /api/proposals/{id}/gate-results`（新設。または #40 に `?history=1`）—— `review_gates` を `(tenant_id, target_type, target_id)` で複数行、`executed_at` 降順。各行 = `contentHash` / 層別 PASS・FAIL / 指摘 / 実行日時 / `HELD` の理由。**再実行で上書きせず履歴として残す**（`F-020 AC-7`。行は既に残っている —— 足りないのは読む API と描く場所）。`S-023` セクション 4 を「現在の結果」から「実行ごとの履歴」に。🔴 **境界は既存 #40 と同じ**（取引先は自社提案分のみ）。🔴 **整合層の機械照合の合否と AI の警告を履歴でも別の見出しで描く**（`docs/02` `ui-design` 申し送り 5。不合格と警告を混ぜない）。 | 結合テスト（同じ提案で内容を変えて 2 回実行 → 2 行が降順で返る / 他社の提案は 404）+ render（履歴が実行ごとに分かれ、警告が不合格と別の見出し）green。`docs/05` §6.5 が先に更新されている。 |
| ③ | T-09-11 ①-③ | `S-023` の履歴は worker の実経路だとゲート・送信の `TRANSITION` 4 本が増える（商談 4 本と合わせて 8 本）。履歴が長くなったときの折りたたみ方針は `docs/04` §S-023 に無い。 | **`ui-design` が `docs/04` §S-023 に折りたたみ方針を追記してから実装する。** 既定案: **§10.3 の共通規約（直近 10 行 + 「すべて表示」）に合流し、`S-023` 固有の例外を作らない**。モバイルのアコーディオン（§S-023「デバイス別」）はそのまま。 | `docs/04` §S-023 に方針がある。render テスト（11 行以上で直近 10 行 + 「すべて表示」/ 10 行以下は全件）green。 |
| ④ | T-09-09 ③-⑤ | 取引先の `S-023` 履歴に**ホストの承認者の表示名**が出る（`users` の C8 DIRECTORY はホスト所属の行を全員に見せる設計であり、`ProposalEvent` の実行者名のための意図された開示）。`approval`（承認記録）は型から外してあるが、履歴の主体名まで伏せるかは `docs/05` §4.4 C8 の判断事項として確認する。 | 🔴 **確認事項。Issue を起票する（`assumption`。[Issue #59](https://github.com/Festal-KM/SES-Platform/issues/59)〔`S-023` の閲覧記録〕と同じ性質 = 開示・記録の範囲の判断）。既定 = 現状維持**（C8 DIRECTORY の設計どおり。`approval`〔承認記録の構造〕は取引先の型に無いまま）。「伏せる」に決まった場合の変更点は `apps/web/lib/proposals/events.ts` の主体名の解決 1 箇所（取引先向け view で実行者名を `ホスト担当者` に丸める）であり、**C8 のポリシーは動かさない**。`program-design` が `docs/05` §4.4 C8 に「`ProposalEvent` の実行者名の開示は意図されたもの（暫定。Issue で確認中）」と注記する。 | Issue が起票され、`docs/05` §4.4 C8 に注記がある。 |
| ⑤ | T-09-11 ②（SP-10 / SP-11 宛。T-12-03 の #23 に直結） | §17.3 #23 前半（AI 上限と HELD → `S-038` の残量表示 → 承認・送信の 409 / 422）はブラウザ経路では未実装のまま結合層にある。ハーネスの env は 1 組で他の全シナリオがゲート PASS を前提にするため、E2E で上限到達を再現するには **spec 単位で `AI_DAILY_COST_LIMIT_USD_DEFAULT` を変えた worker を起動する**か、運営者の `A-004` で上限を下げる経路（Phase 3）が要る。 | **`T-12-03` の証跡リスト（#23 行）で「どう満たすか」を決めて固定する。** 選択肢: **(a)** 専用 spec で上限を下げた worker を起動する（ハーネスの worker は 1 プロセス共有のため Playwright の `projects` / globalSetup の分岐が要る）/ **(b)** 上限到達の**判定**は結合層（`tests/isolation/gate-hold-release.test.ts`）の読み替えを T-12-03 の表に固定し、ブラウザ経路は **`S-038` の残量表示と承認・送信の 409 / 422 の表示**だけを E2E で確かめる（上限到達は `tests/e2e/harness/db-admin.ts` のシームで `usage_counters` を上限値に置く = 「API を通らない経路の模擬」の範囲）。**既定 = (b)**（🔴 **同じ検証を 2 箇所に書かない**。`docs/05` §17.4 の規律 / `docs/dev-plan.md` §8 の読み替えの前例 5 回）。 | `T-12-03` の表の #23 行に採った形（(a) or (b)）が明記され、対応する E2E と結合テストが green。 |

- 🔴 **守ること**: ②は `review_gates` を読む API の追加であり**境界領域として `code-reviewer` 1 回**（取引先が他社提案の履歴に到達しないことを結合テストで固定）。①③④の上流改訂は `ui-design` / `program-design` が先に行い、**改訂前に画面を触らない**。⑤は E2E の割り当ての決定であり、決めた形を `T-12-03` に書く（**本タスクが決め、`T-12-03` が実行する**）。
- **完了の判定**: 上表 ①〜⑤ の「完了の判定」列がすべて満たされている。**①④の Issue 番号を本節に追記する**（`docs/dev-plan.md` §9 と相互参照。どちらか一方にしか無い状態を作らない）。

### T-12-15 🔴 `S-003` / `S-004` の要対応キュー（Phase 1 分）（M）

- **背景**: `docs/sprints/SP-03-auth-audit-admin0.md` T-03-06 は「**承認待ち・送信失敗・公開案件・提案依頼は Phase 1**、`満了間近` は Phase 2 から表示される」と明記し、`apps/web/lib/home/types.ts` の冒頭コメントも同じだが、**`HomeBlock` は `SCAN_QUARANTINE`（T-05-08）のみ**である。SP-09 の T-09-08 ②-④ / T-09-09 ③-④ は「`S-003` の要対応キュー（承認待ち / 送信失敗 → `S-022`）は未実装（**SP-10 の範囲**）」と申し送ったが、**SP-10 に持ち主が無い**（SP-09 §8 ②）。`apps/web/lib/proposals/hrefs.ts` の `proposalApproveHref` の注記「`S-003` の要対応キュー（SP-10）」も同じ誤りである。
- **実装**: `docs/04` §S-003 セクション 1 / §S-004 セクション 1・2 / `docs/05` §6.3 #9（`GET /api/home`。`changedSince` / `rowVersion`）。
  - `HomeBlock`（`apps/web/lib/home/types.ts`）に **追加専用**で `ACTION_QUEUE` を足す（`ScanQuarantineHomeBlock` と同じ作法。**既存メンバーの意味を変えない**）。行 = `{ kind, targetId, subjectLabel（案件名 + 凍結側のエンジニア名）, counterpartyLabel（提案先）, since, deadline, rowVersion, href }`。`apps/web/lib/home/service.ts` / `blocks.ts` / `schemas.ts` に組み立てを足し、`S-003` / `S-004` の画面に描く。
  - **Phase 1 の種別**（`docs/04` §S-003 の 6 種別のうち Phase 1 の 4 つ + 送信保留。並びは「放置時間 × 取り返しのつかなさ」）:

| 種別（`kind`） | 母集団（RLS 適用後） | 遷移先 | 並び |
|---|---|---|---|
| `SEND_FAILED` 送信失敗 | `proposals.state = 'SUBMIT_FAILED'` | `S-022`（**`PROPOSAL_SEND_FAILURES_PATH`**。T-09-08 ②-④） | **最上位**（外部に到達したか不明） |
| `APPROVAL_PENDING` 承認待ち | `state = 'APPROVAL_PENDING'` | `S-021`（`proposalApproveHref`） | 2 |
| `GATE_FAILED` ゲート差し戻し | `state = 'GATE_FAILED'` | `S-020`（`proposalEditHref`） | 3 |
| `SEND_HELD` 送信保留 | `state = 'APPROVED' AND send_hold_reason_key IN ('DOMAIN_UNVERIFIED', 'GATE_STALE')` | `S-019`（`proposalsHref({ state: ['APPROVED'] })`。保留の注記つき一覧） | 4 |
| `PROPOSAL_REQUEST_PENDING` 提案依頼の返答待ち | `proposal_requests.state = 'REQUESTED'`（ホスト = 自社が出した依頼 / 取引先 = 自社宛の依頼） | ホスト `S-017` / 取引先 `S-018` | **期限**（`expiresAt`）昇順 |

  - 🔴 **`SEND_HELD` は `docs/04` §S-003 の 6 種別に無い。先に `ui-design` が §S-003 の種別に `送信保留` を足す**（`送信失敗` と**別の語・別の区分・別の遷移先**。`docs/04` `ui-design` 申し送り 8 / §10.4「失敗率の指標に混入させない」/ SP-09 T-09-06 ③）。🔴 **`PROVIDER_QUOTA` の保留は載せない** —— 利用者に打つ手が無く自動復帰する（`F-059 AC-7` / `hrefs.ts` の `USAGE_SETTINGS_HREF` の注記と同じ理由）。`DOMAIN_UNVERIFIED`（`S-036` で検証する）と `GATE_STALE`（#39 で再実行する）は利用者の操作で解消するため載せる。
  - 🔴 **4 つの「うまくいかなかった」を混同しない**（`GATE_FAILED` / `SUBMIT_FAILED` / `LOST` / `DECLINED`。`F-024 AC-2` / `BR-23` / `BR-60`）: キューに載るのは **`GATE_FAILED` と `SUBMIT_FAILED` だけ**（`LOST` / `DECLINED` は終端であり「対応が要るもの」ではない）。**`DECLINED` / `EXPIRED` / `WITHDRAWN_BY_HOST` の依頼を「返答待ち」の行に残さない**（`REQUESTED` のみ）。**種別ごとの件数を 1 つの合計に丸めない**（丸めると混同の表示になる）。
  - 🔴 **境界**: 母集団は RLS（`proposals` = 作成者 + ホスト、`proposal_requests`）が決め、アプリの `if` で越境の判断を書かない。**取引先の `PartnerHomeView` に他社の件数・存在・順位を示唆するフィールドを足さない**（`apps/web/lib/home/types.test.ts` の型テストを更新して固定）。取引先の行は `docs/04` §S-004 の定義どおり **セクション 1 = 自社宛の `REQUESTED`（返答期限つき。→ `S-018`）/ セクション 2 = 自社提案の `GATE_FAILED`（→ `S-020`）のみ**。**取引先には `APPROVAL_PENDING` / `SEND_FAILED` / `SEND_HELD` を出さない**（承認・送信はホストの工程。取引先は `S-019` の自社提案の状態で足りる）。
  - 🔴 **氏名の出所**: 提案の行（`SEND_FAILED` / `APPROVAL_PENDING` / `GATE_FAILED` / `SEND_HELD`）の「対象」に出すエンジニア名は **`engineer_snapshots.display_name`（凍結側）**であり、`engineers` を読まない（`S-019` の行と同じ出所。`S-003` セクション 0 が氏名を出さないのは**スキルシート文脈の `BR-27` の記録が 60 秒ポーリングで積まれる**ためであり、提案の行には当たらない）。🔴 **ホスト側の `PROPOSAL_REQUEST_PENDING` の行は経路 4 の段階であり、「対象」は案件名 + 匿名候補の参照子（`S-017` と同じ）とし、エンジニア名・`engineerId`・所属会社名を 1 つも載せない**（`ProposalRequest.engineerId` はホスト向け応答に載せない列。`docs/05` §4.6 / `CLAUDE.md` §3.1 経路 4。**`Proposal` が作られるまで実名に到達できない**ことが Phase 1 成功条件 3 そのもの）。`tests/e2e/anonymous-share.spec.ts` の許可キー集合の深さ走査に `GET /api/home` の応答を加える。
  - 「自分の担当のみ」トグルは既定オン（`?scope=mine`。#9 に既にある）。`SALES` は「承認待ち」に自分が承認者でないものも表示する（`docs/04` §S-003 権限差分）。60 秒ポーリングは既存の `changedSince` / `rowVersion` の契約に乗せる（提案の行は `proposals.updated_at`、依頼の行は `proposal_requests.created_at` —— `ProposalRequest` に `updated_at` は無く、`REQUESTED` を離れた時点で行がキューから消えるため版は作成時刻で足りる）。**画面全体を再描画しない**。
  - `hrefs.ts` の「（SP-10）」の注記を `T-12-15` に訂正する。
- **Tier**: `S-003` / `S-004` は **T1（モバイル完結）**。モバイルは 1 行 = **種別バッジ + 対象 + 経過時間**の 3 要素、**セクション 1〜4 を折りたたまない**（`docs/04` §S-003「デバイス別」）。
- **完了の判定**: ①`docs/04` §S-003 に `送信保留` が先に追記されている ②`F-006 AC-1`〜`AC-3` の結合テスト（ホスト 5 種別が並び順どおり / 取引先は 2 種別のみ / 他社の行・件数が 0 / `LOST` `DECLINED` `EXPIRED` が載らない / `PROVIDER_QUOTA` が載らない / `scope=mine` の絞り込み / 🔴 **ホストの `PROPOSAL_REQUEST_PENDING` 行を深さ走査して実名・`engineerId`・所属会社名が 0 件**）green ③型テスト（`PartnerHomeView` に件数示唆のフィールドが無い）green ④render（`S-003` / `S-004`。並び順・空文言「対応が必要なものはありません」）green ⑤E2E `tests/e2e/home.mobile.spec.ts` に「モバイルでセクション 1 が折りたたまれず、行 = 種別バッジ + 対象 + 経過時間」を 1 ケース。
- ✅ **決着（2026-09-17）**: `ACTION_QUEUE` = `{ kind, targetIds, items: { kind, targetId, subjectLabel, counterpartyLabel, since, deadline, rowVersion, href }[] }`（合計の件数フィールド無し・0 件でも必ず返す）。提案の行は `S-019` と同じ射影（`projectProposalListItems`）、依頼の行は `S-017` と同じ写像。`?changedSince=` で `rowVersion >= changedSince` の行だけを返し `targetIds` は常に全件、クライアント（`'use client'` の `ActionQueueSection`）が差分を重ねる。取引先宛の依頼は `scope` で絞らず、`APPROVAL_PENDING` は担当を問わず載せる。🔴 **`scope=mine` の担当は `created_by` / 承認者（`approved_by`） / 自動承認（`approved_by_system`）のいずれかが自分**（レビュー指摘 1。2026-09-17。取引先作成 → ホスト承認は承認者の担当、自動承認は担当不問。`docs/05` §6.3 #9 に反映）。詳細は `docs/05` §6.3 #9「T-12-15 の実装の決着」。結合 `tests/isolation/home-action-queue.test.ts`（21 件）/ E2E `home.mobile.spec.ts` に 1 ケース追加、`isolation.spec.ts` の「取引先の `blocks` は空」を `ACTION_QUEUE` のみ・2 種別のみに改め、`anonymous-share.spec.ts` ④ にホストの `GET /api/home` の深さ走査を足した。🔴 **E2E 3 spec（`home.mobile.spec.ts` / `isolation.spec.ts` ④ / `anonymous-share.spec.ts` ④）は本タスク（レビュー指摘の機械修正。2026-09-17）では未実行**（並走 T-10-09 の WIP migration でハーネス起動不可）。`T-12-03` で実行し実行ログを残す。

### T-12-16 🔴 #46b `GET /api/proposals/{id}/snapshot-diff` と `S-006` セクション 4・5（M）

- **背景**: `docs/05` §6.5 #46b は「**SP-09**」と書くが SP-09 のどのタスクにも無く、`apps/web/app/(main)/engineers/[id]/page.tsx:283` のコメント「提案履歴と凍結差分は SP-09」と `engineer-detail-proposals-coming-soon` のプレースホルダが残っている（SP-09 §8 ③）。`docs/04` §S-006 セクション 4（提案履歴）・5（凍結情報との差分）と `docs/05` §17.3 #25 の差分ビュー部分が **Phase 1 の未実装**として残っている（`F-019 AC-2`）。
- **手順**:
  1. 🔴 **先に `program-design` が `docs/05` §6.5 #46b の「SP-09」を「SP-12（T-12-16）」に改訂する**（`CLAUDE.md` §8.7）。あわせて #46b の境界（下記 🔴 の 404 条件と `engineer.view` の記録）を確定形で書く。
  2. **API #46b**: 応答 `{ frozenAt, fields: {key, frozen, current}[], careers: { frozen: FrozenCareer[], current: CareerRowView[] } }`（`docs/05` §6.5 #46b のとおり）。`fields` のキーは `EngineerSnapshot` の比較可能列（`displayName` / `skills` / `unitPriceMin` / `unitPriceMax` / `availableFrom` / `prefecture` / `remoteMode`）。🔴 **`frozen` と `current` を別のキーで返す。1 つの配列にマージして「変更あり」フラグを立てない**（`docs/05` §6.5「#36 / #46 / #46b の経験内容の凍結」）。`Proposal.offeredUnitPrice`（提案単価）は載せない（差分の対象は**台帳 vs 凍結**のみ）。
  3. **`S-006` セクション 4（提案履歴）**: `listProposals(ctx, { engineerId })`（#45。T-09-09 で `engineerId` フィルタ追加済み）をサーバコンポーネント内で呼ぶ。列 = 提案先 / 案件 / 状態バッジ / 作成日 / `S-023` への導線（`proposalDetailHref`）。空 → 「この人材はまだ提案されていません」（`docs/04` §S-006）。
  4. **`S-006` セクション 5（凍結情報との差分）**: 提案の選択 → #46b → **バージョン差分ビュー**（`docs/04` §5-6。「提案 P-…（凍結日時）↔ 現在」。変更された項目に「提案後に変更」の注記）。経歴は左右に並置（凍結側 = `S-023` セクション 3 と同じ 4 列・同じ並び / 現在値 = セクション 8 と同じ）。
  5. プレースホルダ `engineer-detail-proposals-coming-soon` と `engineers.detail.proposals.comingSoon` の文言を除去する。🔴 **testid の削除は `tests/static/testid-inventory.test.ts` に当たる** —— **意図した削除として同じコミットでインベントリを更新し、理由（プレースホルダの実装置換）をコミットに書く**（SP-21 の規律は「黙って壊さない」であり、「永久に消せない」ではない）。
- 🔴 **守ること**:
  - **経路 2 の範囲を超えない。** #46b は `withTenant` の 1 トランザクションで `proposals` + `engineer_snapshots`（凍結側）と `engineers` + `engineer_careers`（現在値。**C3 OWNER_SCOPED**）を読み、🔴 **現在値の行が読めなければ 404**（`docs/05` §4.8「見えない ＝ 存在しない」）。**凍結側だけを返す形にしない** —— 返すと #46 と二重になり、「現在値が無い = 他社所有」の示唆になる。したがって**ホストが取引先所有エンジニアの提案（経路 2）で #46b を叩いても 404**、取引先は**自社提案 × 自社エンジニア**でのみ 200。`S-006` は実行者の文脈で C3 が返すエンジニアだけが開ける既存の境界を変えず、セクション 4 / 5 はその中で描く。
  - 🔴 **`engineer.view` の記録**: `S-006` は既に閲覧を記録する（T-05-02）が、#46b は API として直接叩けるため、**同一トランザクションで `engineer.view`（`summary: { via: 'snapshot-diff', proposalId }`）を記録し、記録に失敗したら返さない**（`docs/05` §16.1 / §6.1 K-7 と同じ規律。**現在値の再読は「エンジニア詳細の閲覧」である**）。
  - **`EngineerSnapshot.careers` は `app_platform` に `GRANT` されていない**（`docs/05` §6.5）。#46b は主平面の API であり、管理平面から呼べる経路を作らない。
  - **セクション 4 の行は `S-019` の行と同じ出所・同じ射影（`HostProposalListItem` / `PartnerProposalListItem`。`listProposals`）を使い、`S-006` 固有の射影を作らない**（2 実装にすると片方だけ境界を見ない経路になる。T-09-09 が `listProposalSendFailures` を `listProposals` に統合したのと同じ理由）。
- **完了の判定**: ①`docs/05` §6.5 #46b が「SP-12（T-12-16）」に改訂され、404 条件と記録の規律が書かれている ②`F-019 AC-2` の結合テスト（凍結後に台帳の 7 項目を変更 → `fields[]` の `frozen` ≠ `current` / 経歴を 1 行編集・1 行削除・1 行追加 → `careers.frozen` は不変で `careers.current` だけ変わる / **ホストが取引先所有エンジニアの提案で 404** / 取引先が他社提案で 404 / `engineer.view` の記録失敗で応答が返らない）green ③`S-006` の render（セクション 4 の空文言 / セクション 5 の左右並置。**1 つのリストに混在していない**）green ④`docs/05` §17.3 #25 の差分ビュー部分が E2E か結合で green（読み替えを `T-12-03` の表の #25 行に記す）⑤`engineer-detail-proposals-coming-soon` が DOM に無く、インベントリが意図した削除として更新されている。
- ✅ **決着（2026-09-17）**: #46b = `apps/web/lib/proposals/snapshot-diff.ts`（`withTenant` 1 tx で `proposals`〔C5〕→ `engineer_snapshots` → `engineers`〔C3。`null` なら 404〕→ `recordEngineerView(via='SNAPSHOT_DIFF', { proposalId })` → 経歴・スキル）+ `app/api/(main)/proposals/[id]/snapshot-diff/route.ts`（`guards: []`）。応答の組み立ては純粋関数 `snapshot-diff-fields.ts`（7 キー固定 / `frozen`・`current` 別キー / 現在値のスキルを凍結の形に写す）。`S-006` セクション 4 = `listProposals(ctx, { engineerId })` の行、セクション 5 = `?diff=<proposalId>` で選んだ提案を**同じ関数**で読み、`engineer-proposal-sections.tsx` が凍結側の表と現在値の表を左右に並置（「提案後に変更」は画面側の突き合わせ `snapshotFieldEquals` / `careersEqual`）。プレースホルダ `engineer-detail-proposals-coming-soon` / `engineers.detail.proposals.comingSoon` を除去しインベントリを更新。`docs/05` §6.4 許可リスト表・§16.1 に `SNAPSHOT_DIFF` / #46b を追記。詳細は `docs/05` §6.5「T-12-16 の実装の決着」。結合 `tests/isolation/proposal-snapshot-diff.test.ts`（11 件。⑥ = `audit_logs` の INSERT 権限を外して 500）/ ユニット 2 本 / render 1 本 / E2E `audit-k7.spec.ts` ⑤（#25 の読み替えは上表）。

### T-12-17 🔴 SP-10 / SP-11 からの申し送りの回収 ①（コード / テストの是正。上流改訂・人間判断を要しないもの）（M）

- **背景**: SP-10 / SP-11 の完了確認（2026-09-17。いずれも `PHASE_INCOMPLETE`。**理由の大半は計画側の転記漏れ** —— 各タスクの決着行が「SP-12 へ」「SP-12 で」と申し送ったのに、本スプリントに受け皿が無かった）で挙がった項目のうち、**上流（`docs/04` / `docs/05`）の改訂や人間判断を要しないコード / テストの是正**を回収する。残り（上流改訂・人間判断を要するもの）は `T-12-18`。🔴 **原文は SP-10 / SP-11 から写したものであり、言い換えない**（出典の節を消さない。本表が「何を回収したか」の照合先になる。`T-12-13` と同型）。🔴 **いずれも判定を緩めない・許可リストを広げない・testid を削らない**方向の是正であり、**逆方向（緩める）に転ぶ変更を本タスクに混ぜない**。
- **原文と、何をどう直すか**:

| # | 出典 | 原文（SP-10 / SP-11 より） | 何をどう直すか | 完了の判定 |
|---|---|---|---|---|
| ① | SP-11 T-11-11 レビュー申し送り ① | `engineer-share-screen.tsx` の `削除済み`（`RowMark: 'DELETED'`）は `hidden sm:table-cell` の状態列にしか描かれず、モバイル（`sm` 未満）では操作ボタンが消えるだけで理由が見えない（`docs/04` §10.1 / `CLAUDE.md` §13.3）—— 操作セルにも `stateDeleted` を描く（404 競合時のみの経路） | `apps/web/app/(main)/engineer-shares/engineer-share-screen.tsx` の操作セルに、`RowMark: 'DELETED'` のとき `stateDeleted` の文言（`packages/i18n` の既存キー）を描く。**状態列の描画は変えない**（デスクトップで 2 箇所に同じ語が出てよい。判断材料を隠さないほうを採る）。testid は追加のみ。 | render テスト（`S-015`。`DELETED` の行で `sm` 未満相当の描画に理由の文言が在る）green。`tests/static/testid-inventory.test.ts` 不変。 |
| ② | SP-11 T-11-11 レビュー申し送り ② | `service.ts` の `decodeEngineerShareCursor` を `withTenant` の内側で呼んでいるため、400 になる要求でもトランザクション + `SET LOCAL` を開く —— `assertPartnerContext` の直後へ移す | `apps/web/lib/engineer-shares/service.ts` で `decodeEngineerShareCursor` の呼び出しを `withTenant` の外（`assertPartnerContext` の直後）へ移す。**戻り値・エラーコード（400）は変えない。** | 既存の結合 `tests/isolation/engineer-shares.test.ts`（30 件）green。ユニットに「不正カーソルで `withTenant` が呼ばれない」を 1 件足す（`withTenant` をスパイ）。 |
| ③ | SP-11 T-11-11 レビュー申し送り ③ | `i18n` の `次の 50 件` は `PAGE_SIZE_DEFAULT`（`packages/config/src/limits.ts`）と手で同期 —— `engineer-share-list-contract.test.ts` に `expect(PAGE_SIZE_DEFAULT).toBe(50)` と文言の対照を 1 件足す | `tests/static/engineer-share-list-contract.test.ts` に原文どおり 1 件足す（値と文言の対照）。**文言を `t()` の引数で組み立てる形に変えない**（`T-10-01` の決着「`t()` に引数は足さない」）。 | 静的テスト green。`PAGE_SIZE_DEFAULT` を変えるとこのテストが落ちる（空振り検査 1 回）。 |
| ④ | SP-11 T-11-11 レビュー申し送り（末尾） | 加えて `docs/04` §S-015 の氏名セルへの名称規約（`NameCell`）の適用は未着手（`20f22b9` に記録）。 | `S-015` の氏名セルを `@ses/ui` の `NameCell`（`T-11-12` の決着。`lg` 以上 = 切り詰め + `title` / `lg` 未満 = 折り返し / **`href` 無しは切り詰めない**）に寄せる。**`S-015` の氏名に導線は無い**ので `href` 無し = 切り詰めない側で描く（規約どおり）。**`docs/04` §S-015 は改訂 12 で `NameCell` 適用が定まっているため上流改訂は不要**（`docs/04` §10.3 / §11 の T-11-12 の決定に合流するだけ）。 | render テスト（`S-015` の氏名セルが `NameCell` 経由）green。`expectNoBrokenLabels` / `expectNoHorizontalOverflow` 無改変で `home.mobile.spec.ts` green。 |
| ⑤ | SP-11 T-11-04 レビュー申し送り ① | `apps/worker/src/jobs/send-hold-release.ts` に `ProviderQuotaNearingMarker.observe()` を足す（現状は誰も `A-005` を開かないと接近時刻の目印が維持されず TTL 24h で消える。T-09-08 後） | `send-hold-release.ts` が既に計算している `warning`（`isProviderQuotaWarning`）の結果を `ProviderQuotaNearingMarker.observe()`（`@ses/connectors` の `createRedisProviderQuotaNearingMarker`）に渡す。**表示専用の揮発値であり、テナントの業務データを書かない**（T-11-04 決着の位置づけを変えない）。 | `apps/worker/src/jobs/send-hold-release.test.ts` に「`warning` 真で `observe(true)` / 偽で `observe(false)` が 1 回ずつ」を足して green。 |
| ⑥ | SP-11 T-11-04 レビュー申し送り ② | `packages/connectors/src/bullmq.ts` の `queue.getFailed(0, 0)` は全キューで `Job` ハッシュ全体（`account.mail` の `data.token` を含む）を Redis から取得してから時刻だけを写している。応答・ログには出ないが、failed ZSET のスコアだけを読む形に寄せられる | `createBullMqFailedJobsReader` を **failed ZSET のスコア（`finishedOn`）だけを読む**形に寄せ、`gate.run` 以外のキューでは `Job` ハッシュを取得しない（`account.mail` の平文トークンがプロセスのメモリに乗る経路を消す）。🔴 **読むだけの口という性質は変えない**（`Job.retry()` / `remove()` を持つオブジェクトを返さない。`tests/static/admin-no-gate-retry.test.ts` ①）。 | `packages/connectors` のユニット（`bullmq.test.ts`。`account.mail` の failed が在っても `data` を読まない = Redis の `HGETALL` が呼ばれない）green。結合 `tests/isolation/admin-monitoring.test.ts` の項目 3 / 12 が不変で green。 |
| ⑦ | SP-11 T-11-04 レビュー申し送り ③ | `readers.ts` の `readMailProviderHeld`（DB）失敗が `PROVIDER_READ_FAILED`（「Redis を読めませんでした」）になり原因を誤案内する → `DB_READ_FAILED` に分ける | `apps/web/app/api/admin/monitoring/_lib/readers.ts` で `readMailProviderHeld` の失敗を `errorKind='DB_READ_FAILED'` に分け、`packages/i18n` に文言を 1 キー足す（`A-005` の「上限を確認できていません」とは別の語）。`errorKind` の閉集合（`apps/web/lib/admin-monitoring/view.ts`）に **追加のみ**。 | ユニット（`readers` の失敗注入で `DB_READ_FAILED` / `getQuota()` の失敗で `PROVIDER_READ_FAILED` が別々に返る）+ render（文言が別）green。 |
| ⑧ | SP-11 T-11-04 レビュー申し送り ④ | `bootstrap.quotaEmailSender()` が `quotaCache` を渡していないためプロセス内 60 秒キャッシュ（設計は Redis の 60 秒キャッシュ。worker も同じで T-11-04 の退行ではない） | `apps/web/lib/db/bootstrap.ts` の `quotaEmailSender()` と `apps/worker` の同じ生成箇所に `quotaCache`（Redis）を渡す。**キャッシュの TTL（60 秒）は変えない**（`docs/05` §8.3-Q）。 | ユニット（`quotaCache` が渡され、2 回目の `getQuota()` が Redis の値を返す）green。 |
| ⑨ | SP-11 T-11-04 レビュー申し送り ⑤ | 結合テストは `bootstrap` をモックしているため `monitoringRuntime()` の実配線は typecheck のみ。`development` で `GET /api/admin/monitoring` を 1 回叩く smoke を T-11-07 で。 | ✅ **実質的に済んでいる（読み替え）**: T-11-07 の E2E #15 `tests/e2e/admin-non-disclosure.spec.ts` (c) が **`GET /api/admin/monitoring` の 15 項目すべてが `ok: true`** であることを `development` の実配線（ハーネスの web は `bootstrap.ts` の `monitoringRuntime()` を通る）で確かめている（`docs/05` §17.3 #15 (c)）。**新しいテストは足さない** —— 本行は「T-11-07 で」の申し送りが**どこで満たされたか**を名指しするためだけに残す。 | `admin-non-disclosure.spec.ts` (c) が `T-12-03` の実行ログに含まれる（追加作業なし）。 |
| ⑩ | SP-10 T-10-02 → SP-11 T-11-04 申し送り ④ | `usage-storage-reconcile` の解消スコープが当月キーのみのため、月末日に開いた `STORAGE_DIVERGENCE` が翌月 1 日に一致した場合、前月キーの行が閉じられず残る（前月キーもスコープに含めれば閉じる。SP-11 で直してよい） | `packages/db/src/usage-storage-reconcile.ts`（純粋部分は `packages/domain/src/usage/storage-reconcile.ts`）の解消スコープに **前月キー**を含める。🔴 **`usage_counters` を 1 バイトも動かさない**（`docs/05` §9.8.1 ③。閉じるのは `usage_measurement_findings.resolved_at` だけ）。 | 結合 `tests/isolation/usage-measurement.test.ts` に「月末日に開いた行が翌月 1 日の一致で閉じる」を 1 件足して green。 |
| ⑪ | SP-11 T-11-07 申し送り ②（= T-11-02 申し送り ⑥） | `A-004` のモバイルビューポート（T-11-02 申し送り ⑥）は E2E 未追加（`*.mobile.spec.ts` を SP-12 で）/ ⑥モバイルビューポートでも `A-004` の表が `hidden` にならず横スクロールで読めること（T3。`CLAUDE.md` §13.3） | `tests/e2e/admin.mobile.spec.ts`（新設。管理平面の T3 画面用。Pixel 5）に `A-004` の 1 ケース（表が `hidden` でなく横スクロールで全列に到達できる）を足す。**既存の禁止値走査（`admin-non-disclosure.spec.ts`）と重複させない**（`docs/05` §17.4 の規律）。 | E2E green（`expectNoBrokenLabels` 無改変）。 |
| ⑫ | SP-11 T-11-09 申し送り ⑤ | `S-041` のモバイル E2E（`*.mobile.spec.ts`。行の展開が縦積みで読めること）は未追加。既存 `audit-k7.spec.ts` は API 経路のみ | `tests/e2e/settings.mobile.spec.ts`（`S-041` は設定系の主平面画面）に 1 ケース（行の展開が縦積みで読め、`detail` の 7 種の固定形 DTO のうち少なくとも `project.visibility_change` の before / after が読める）。 | E2E green。 |
| ⑬ | SP-11 T-11-07 申し送り ⑥ | `admin-forbidden-keys.test.ts` の API-A8 の静的例外は `FILE_RESPONSE` でファイル単位に効くため、ファイル内の全 export 型が対象になる。型名を `^GateStall` に限定する余地がある | `tests/static/admin-forbidden-keys.test.ts` の API-A8 の例外（`targetId` + `reason`）を **型名 `^GateStall` に限定**する（ファイル単位 → 型単位）。🔴 **例外を狭める方向の変更であり、広げない。** 同じ定数を `admin-non-disclosure.spec.ts` も import しているため両方が同時に厳しくなる。 | 静的テスト green（限定後に `apps/web/lib/admin-monitoring/view.ts` の他の export 型で `targetId` が出れば落ちることの空振り検査 1 回）。 |
| ⑭ | SP-11 T-11-07 申し送り ⑦ | 走査区画（`packages/db/src/platform/queries/*.ts` / `packages/db/src/serializers/platform/*.ts` / `apps/web/lib/admin-*/view.ts`）に `apps/web/app/api/admin/**/route.ts` の export 型（`*Response`）を加える余地がある | 同テストの走査区画に `apps/web/app/api/admin/**/route.ts` の export 型（`*Response`）を **追加**する。 | 静的テスト green（追加した区画で禁止キー 0 件）。 |
| ⑮ | SP-11 T-11-09 申し送り ② | `PROPOSAL_REQUEST_OPERATIONS`（`packages/domain`）への書き込み側 2 箇所（`apps/web/lib/proposal-requests/service.ts` / `packages/db/src/proposal-request-expiry.ts`）の参照の寄せ | 2 箇所の `operation` リテラルを `PROPOSAL_REQUEST_OPERATIONS` の定数参照に寄せる。**値は変えない**（監査ログの改変にならない）。 | 静的テスト（`proposal_request.*` の `summary.operation` にリテラルを書く箇所が非テストソースに 0 件）を 1 本足して green。 |
| ⑯ | SP-11 T-11-09 申し送り ⑨（任意） | `values` を持たない `ENUM` 17 キーは大文字スネーク検査で通している —— `beforeRole` / `afterRole` に `TenantRole`、`via` に `ENGINEER_VIEW_VIA` / `PROJECT_VIEW_VIA` の閉集合を与える余地。 | **任意（時間があれば）。** `packages/domain/src/audit/pick-detail.ts` の当該 3 キーに閉集合を与える（`AUDIT_DETAIL_ALLOWLIST` のスナップショット更新は**キーの追加ではなく値集合の厳格化**。🔴 **キーを増やさない**）。 | スナップショット更新 + 閉集合外の値が落ちるユニット 1 件。見送るなら「見送り（任意）」と本表に残す。 |
| ⑰ | SP-11 T-11-05 申し送り ⑦（任意） | `PROJECT_PUBLISH` の行は対象の生存を確認していない（`project_publish_requests` に `app_platform` の GRANT が無い）。案件が削除された後の failed 記録が残ることがある。気になるなら SP-12 で `project_publish_requests(tenant_id, project_id, content_hash, requested_at)` に列 GRANT を足して母集団を絞る | **任意。** 足す場合は列レベル `GRANT` を原文の 4 列に限り、`docs/05` §5.5 の表と `platform-grants.ts` / `tests/static/platform-grants-consistency.test.ts` を同時に更新する（🔴 **GRANT を足す変更は `docs/05` §5.5 が先** —— この 1 点だけは上流の追記を伴うが、`BR-40` の非開示列に触れない 4 列であり `program-design` の表の 1 行追加で済む）。 | 見送るなら「見送り（任意）」と本表に残す。足すなら `platform-grants-consistency.test.ts` green + 結合（削除済み案件の failed が項目 12 から落ちる）。 |
| ⑱ | SP-10 T-10-09 → T-10-10 への申し送り（= SP-10 完了確認 T-10-09 ②） | `RUNNING` が残った行（④成功 → ⑤失敗のとき）は「未完了」として扱う。`S-042` / `A-003` / `A-013` / `A-005` には完了の事実を出していない（`A-005` 項目 7 は `FAILED` だけ）。 | `A-005` 項目 7（`kind='PURGE_JOB_FAILED'`）に **`TenantPurgeRun.status='RUNNING'` が閾値（`SUBMITTING_STALL_ALERT_MINUTES` と同型の新キー `PURGE_RUN_STALL_ALERT_MINUTES`。`packages/config`）を超えて残る行**を `cause='RUNNING_OVERDUE'` として **同じ項目の別区分**で足す（T-11-04 の追補。`FAILED` と混ぜない / 項目 15〔予告待ち〕とも混ぜない）。🔴 **完了の事実は引き続き出さない**（`docs/05` §6.9「作らないもの」。`A-010` の 1 本だけ）。表示は件数・状態・時刻のみ。 | 結合 `tests/isolation/admin-monitoring.test.ts` に「`RUNNING` 滞留が項目 7 に `RUNNING_OVERDUE` で出て、`FAILED` の件数に加算されず、`counts` / `failure_reason` が応答に無い」を足して green。`docs/04` §A-005 項目 7 の 1 行追記は **`ui-design`**（項目 16 / 17 と同じ扱い。表示区分の追加のみ）。 |
| ⑲ | [Issue #68](https://github.com/Festal-KM/SES-Platform/issues/68)（2026-09-17 起票。既定で進行） | CSV 数式注入の無害化 + `TENANT_PURGE_GRACE_DAYS` の本番固定（既定で進行） | (a) `apps/worker/src/jobs/export-generate.ts`（返却 CSV。`F-064 AC-5`）で **先頭が `=` / `+` / `-` / `@` / タブ / CR のセル値を無害化**する（先頭に `'` を付ける方式。`packages/domain` の純粋関数 1 つに閉じる）。**値そのものは変えない**（返却データの改変にならない範囲）。(b) `packages/config/src/schema.ts` の `TENANT_PURGE_GRACE_DAYS` を **`production` では 30 に固定**（`APP_ENV=production` で 30 以外なら起動失敗。`SEED_DATABASE_URL` の環境制限〔§13.4 規則 7〕と同型）。**非本番では従来どおり可変**（E2E #24 が短い値を使う）。 | (a) ユニット（4 種の先頭文字 + 通常値の対照）+ 結合（`export.generate` の出力に生の `=SUM(` が無い）green。(b) `packages/config/src/schema.test.ts` に `production` × 29 で起動失敗 / `development` × 1 で通る、を足して green。 |
| ⑳ | [Issue #67](https://github.com/Festal-KM/SES-Platform/issues/67) B（= SP-10 T-10-09 決着の「オーケストレーターへ」②。**回答待ち**） | ② **`retain` に置いた**が確認が要るもの = `tenant_esign_connections`（暗号化済みトークンを `PURGED` で失効させるべきか）/ `two_factor_credentials` / `projects.end_client_name`（商流は個人情報ではないとして残置） | 🔴 **回答が来てから着手する**（既定 = 現状〔`retain`〕で進行。**推奨 = 削除**）。回答が「削除」なら `packages/config/src/retention.ts` の `PURGE_SPEC` で `tenant_esign_connections` / `two_factor_credentials` を **`delete` へ移し**、`provisional: 'ISSUE-67'` を外す。`projects.end_client_name` は回答に従う。**静的 `purge-spec-coverage.test.ts` は表の移動で自動的に追随する**（全業務テーブルがどちらかに現れる、の検査は変わらない）。 | 回答が来ていなければ「回答待ち（[#67](https://github.com/Festal-KM/SES-Platform/issues/67)）」と本表に残して本タスクを閉じてよい。回答後: 結合 `tests/isolation/tenant-purge.test.ts` に `PURGED` 後の 2 表の行数 0 を足して green。 |
| ㉑ | SP-10 T-10-01 SP-12 への申し送り ①（低優先） | **ESLint `no-restricted-syntax`（`JSXText` / JSX 属性の日本語）を `apps/web` に足す** —— `eslint.config.mjs` の CATCH_ALL / ADMIN_PLANE / SHARED_CANDIDATE_CALLER の 3 ゾーンに共通の選択子ビルダを足す形で（新ブロックを作ると既存の禁止が消える）。静的テスト #36 が同じ強度を持つので急がない | **低優先（任意）。** 足す場合は原文どおり **3 ゾーン共通の選択子ビルダ**で（新ブロックを作らない）。🔴 **既存の禁止（`@ses/db` の生 `PrismaClient` / 管理平面の import 境界 / 共有候補の呼び出し元）が 1 つも消えないこと**を、既存の lint 対照テストで確認する。 | `pnpm lint` green + 既存の禁止 3 種の対照ファイルが引き続き lint エラーになる（空振り検査）。見送るなら「見送り（低優先）」と本表に残す。 |

- 🔴 **守ること**: ⑥⑦⑧⑨⑬⑭⑱ は管理平面 / 外部送信の周辺であり**境界領域として `code-reviewer` 1 回**（`CLAUDE.md` §8.3）。⑲(b) は `packages/config` の起動時検証（環境分離）であり同じく 1 回。①②③④⑪⑫⑮⑯ は UI / テストのみ。⑳ は回答待ちを理由に本タスクを止めない。🔴 **どの項目も「判定を緩める」「許可リストを広げる」「testid を削る」方向に転ばない**（⑬⑭ は例外を狭める / 区画を広げる方向、⑲(b) は固定する方向）。
- **完了の判定**: 上表 ①〜㉑ の「完了の判定」列がすべて満たされている。🔴 **任意の 3 件（⑯⑰㉑）と回答待ちの 1 件（⑳）は、実施しない場合も「見送り（任意）」「回答待ち」と本表に書き残して閉じる**（未消化のまま消さない）。

### T-12-18 🔴 SP-10 / SP-11 からの申し送りの回収 ②（上流改訂・人間判断を要するもの）（M）

- **背景**: `T-12-17` と同じ（SP-10 / SP-11 の完了確認。2026-09-17）。本タスクは **`docs/04` / `docs/05` の改訂が先に要るもの、または人間判断（Issue）を要するもの**を回収する。🔴 **順序は上流から**（`CLAUDE.md` §8.7。**スプリントファイルとコードだけを直さない**）。🔴 **Issue の起票はオーケストレーターが行う**（`CLAUDE.md` §8.6。本タスクの `programmer` は「既定」で進め、回答を待って止まらない）。🔴 **`S-041` の許可リストに関わる項目（⑨⑩⑫）は必ず `docs/04` §S-041 の表 → `docs/05` §6.4 許可リスト表 → `AUDIT_DETAIL_ALLOWLIST`（`packages/domain/src/audit/pick-detail.ts`。スナップショット更新）の順で行う** —— SP-11 T-11-09 申し送り ① が定めた順序であり、**許可リストへ直接足すと `docs/04` と `docs/05` が実装より後ろになる**。
- **原文と、何をどう直すか**:

| # | 出典 | 原文（SP-10 / SP-11 より） | 何をどう直すか（既定と選択肢） | 完了の判定 |
|---|---|---|---|---|
| ① | SP-11 T-11-01 申し送り ② | `docs/04` §A-002 の「並び替え → 異常度 / 最終アクティビティ / 席利用率 / エンジニア数」に対し実装は `health` / `name` / `createdAt`（`docs/05` の決着表に注記）。`ui-design` の改訂で確定させる（T-11-03 の A-006 フィルタ差分と同じ扱い） | **`ui-design` が `docs/04` §A-002 の並び替えを実装（`health` / `name` / `createdAt`）に合わせて改訂する**（既定 = 実装を正とする。異常度以外の並びは §A-002 の目的〔異常なテナントを上位に〕に寄与しないため）。追加の並び（最終アクティビティ / 席利用率）が要ると判断した場合のみ API-A2 に `sort` 値を**追加**する（`docs/05` §6.9 が先）。 | `docs/04` §A-002 が改訂されている。追加する場合は結合 `tests/isolation/admin-tenant-health.test.ts` に並びの決定性を足して green。 |
| ② | SP-11 T-11-01 申し送り ③ | `docs/04` §A-002 の異常の種別にある「未対応の送信失敗あり」はスコアに入れていない（`A-005` 項目 1 と二重計上になる。足すなら `docs/04` / `docs/05` §5.7 を先に改訂） | **既定 = 入れない。** `ui-design` が `docs/04` §A-002 の異常の種別から「未対応の送信失敗あり」を外す（または「`A-005` 項目 1 で扱う」と注記する）。二重計上を避ける根拠を §11 に 1 行残す。 | `docs/04` §A-002 が改訂されている。 |
| ③ | SP-11 T-11-01 申し送り ④ | `docs/04` §A-002 セクション 1「異常の要約（種別ごとの件数。クリックで絞り込み）」と「閾値を割った件数を最上部の要約に出す」は未実装（本タスクは列の追加と並び替えのみ）。全件の材料はクエリ内に揃っているので、応答に `summary: Record<TenantHealthSignal, number>` を足せば実装できる。要否は `ui-design` / `pm` で | 🔴 **既定（`pm`。原文が「要否は `ui-design` / `pm` で」と委ねた分）= Phase 1 で実装する**（材料が揃っており、`F-056 AC-2`「異常なテナントを上位に出す」の要約はリリース直後の日次監視〔`docs/dev-plan.md` §2.2 `RL-12` / §6.4 R-16〕で運営者が最初に見る値である。**新スコープではなく `docs/04` §A-002 に定めがある**。`ui-design` が「Phase 2 でよい」と判断した場合は §A-002 のフェーズ注記を改訂して見送る）。順序: `docs/05` §6.9 API-A2 の応答に `summary: Record<TenantHealthSignal, number>` を**追加**（`program-design`）→ 実装（`A-002` セクション 1 の要約 + クリックで `?signal=` の絞り込み）。**件数だけであり、テナントの内容には立ち入らない**（`BR-40`）。 | `docs/05` §6.9 が先に更新されている。結合（`summary` の各値が一覧の集計と一致 / 絞り込みで他の種別が 0 件）+ render（要約が最上部）green。E2E #15 の禁止キー走査に `summary` のキー集合（列挙値のみ）が通る。 |
| ④ | SP-11 T-11-02 申し送り ⑤ | `docs/04` §A-004 の「クォータ設定は件数と金額上限を別の入力として持つ」のうち**金額上限の入力は SP-20**（本タスクは **AI の月次件数 4 単位のみ**。`EMAIL_COUNT` / `STORAGE_BYTES` は上書きの対象に含めない。NG-1 で 6 計測から修正。理由と後続タスクは⑥）—— `ui-design` で `docs/04` に注記を。 | `ui-design` が `docs/04` §A-004 に「金額上限（`quotaOverrideUsd`）の入力は Phase 3（SP-20）。Phase 1 は AI の月次件数 4 単位（+ `T-12-12` でメール / ストレージ）」を注記する。**実装は変えない。** | `docs/04` §A-004 に注記がある。 |
| ⑤ | SP-11 T-11-03 申し送り ① | `docs/04` §A-006 のフィルタとの差分（`actorType` のみ / `targetType` 未実装 / `deviceKind` 追加）は `docs/04` に注記済み。`ui-design` 改訂で確定させる | `ui-design` が `docs/04` §A-006 のフィルタを実装（`actorType` / `deviceKind` あり、`targetType` なし）に合わせて確定する（既定 = 実装を正とする）。`targetType` を要すると判断した場合のみ API-A7 に**追加**（`docs/05` §6.9 が先。`T-12-13` ① の `target_type` 統一が前提）。 | `docs/04` §A-006 が改訂されている。 |
| ⑥ | SP-11 T-11-03 申し送り ③ | `reason` を内容キーとして落とすため `auth.login_failed` の `PASSWORD_MISMATCH` / `USER_DISABLED` の区別が `A-006` から消える。必要なら列挙値だけ残す形状判定へ | 🔴 **確認事項（`assumption`）。既定 = 現状維持**（安全側。区別が要る調査は `A-006` ではなく Sentry / DB の直接照会で行う）。「残す」に決まった場合は `packages/domain` の `maskAuditSummary()` で **`auth.login_failed` の `reason` に限り列挙値の閉集合（`PASSWORD_MISMATCH` / `USER_DISABLED` / …）だけを通す**形状判定に変える（自由記述は引き続き `[masked]`）。順序は `docs/05` §5.5（シリアライザの表）→ 実装。 | Issue が起票され、`docs/05` §5.5 に「暫定。Issue で確認中」の注記がある。 |
| ⑦ | SP-11 T-11-03 申し送り ④ | `withPlatformWrite` の `before` / `after`（`JSON.stringify` 文字列）は 64 文字超で `[masked]` に畳まれ、`admin.tenant.create` の内容が `A-006` から読めない。安全側だが障害調査の材料としては T-11-07 で扱う | **T-11-07 では扱われなかった**（T-11-07 決着に記載無し）。🔴 **確認事項（⑥と同じ Issue にまとめてよい）。既定 = 現状維持。** 「読めるようにする」なら `withPlatformWrite` の `summary` を文字列化した `before` / `after` ではなく **キーごとの構造**（`docs/05` §6.4 の許可リスト方式と同型）で書くように**記録側**を直す（画面で復元しない）。順序は `docs/05` §16.1（`admin.*` の `summary` の形）→ 実装。 | Issue が起票され、`docs/05` §16.1 に注記がある。 |
| ⑧ | SP-11 T-11-07 申し送り ① + ⑧ | ①GRANT が残る自由記述・名称列の要否が未判定 —— `contracts.withdraw_reason`（Phase 3）/ `project_requirements.free_text` / `projects.name` / `public_summary` / `scheduler_runs.detail`。§5.7 は `A-002` / `A-003` に案件名を見せないと定めるが、Phase 2 の代理閲覧（`A-007`）が `projects.name` を使うかで判断が変わる / ⑧①の「GRANT が残る自由記述・名称列」の候補に `engineer_skills.original_label` / `skill_aliases.alias` を加える。 | 🔴 **`program-design` の判断事項（`docs/05` §5.5 の表）。既定 = Phase 1 では REVOKE しない（現状維持）** —— 代理閲覧（`A-007`。SP-16 `F-060`）は read-only ロールで主平面の画面をそのまま見る設計であり `app_platform` の GRANT を使わない見込みだが、**Phase 2 の再計画で `A-007` の DB ロールを確定させるまで判断を保留**する。`program-design` が `docs/05` §5.5 の表に 7 列（原文の 5 + ⑧の 2）を「**未判定。`A-007` の設計時に決める**」として列挙する（列挙が無いと忘れられる）。 | `docs/05` §5.5 に 7 列の未判定リストがある。Phase 2 再計画（`docs/dev-plan.md` §3.3.1）の入力として `docs/dev-plan.md` §8 に記録されている。 |
| ⑨ | SP-11 T-11-07 申し送り ④ + T-11-09 申し送り ① | ④`A-006` の `summary` に `modelId` / `promptVersion` / `purpose` / `targetId` が列挙値 / ID として残るのは `maskAuditSummary` の ③ どおり（T-11-02 申し送り「`A-006` の `summary` は別」）。T-11-09 の棚卸しで許可リスト方式に変えるなら、ここで固定した「群 D は残ってよい」を同時に見直す / ①**許可リストに無いため落ちる値を出したい場合の順序**は `docs/04` §S-041 の表 → `docs/05` §6.4 許可リスト表 → `AUDIT_DETAIL_ALLOWLIST`（スナップショット更新）。候補: `proposal.update` の `GATE_RESULT` の各層 verdict（`overall` / `piiVerdict` / `commerceVerdict` / `consistencyVerdict` / `findingCount` / `warningCount`）、`proposal.update` `DRAFT_UPDATE` の `fields`（項目名の列挙）、`skill_sheet.*` の `wasLatest`、`state.invalid_transition` の `entity` / `from` / `to` | **群 D（`A-006` 側）**: 既定 = 現状維持（列挙値 / ID は残ってよい。`BR-40` の非開示は「内容」であり列挙値は内容ではない）。`program-design` が `docs/05` §5.5 に「群 D は残ってよい。`S-041` の許可リストとは独立に判断する」と明記する。**追加候補（`S-041` 側）**: 🔴 **`ui-design` が `docs/04` §S-041 の表に候補 4 群を載せるかを決める**（既定 = **`GATE_RESULT` の各層 verdict と `state.invalid_transition` の 3 キーは載せる**〔承認の根拠と不正遷移の監査は `BR-27` の目的そのもの〕/ `DRAFT_UPDATE` の `fields` と `wasLatest` は載せない〔運用上の必要が出るまで〕）→ `docs/05` §6.4 許可リスト表 → `AUDIT_DETAIL_ALLOWLIST` のスナップショット更新。🔴 **未知のキーは既定で出さない構造を変えない。** | `docs/04` §S-041 → `docs/05` §6.4 → `pick-detail.ts` の順で更新されている（3 箇所の差分が同じコミット列に在る）。結合 `tests/isolation/audit-log-detail.test.ts` に追加キーの正のケースと、載せないキーが落ちる負のケースが在り green。 |
| ⑩ | SP-10 T-10-09 決着の「オーケストレーターへ」③ に準ずる / SP-10 完了確認 T-10-09 ③ | `tenant.purge` / `data_export.download` の `summary` を `S-041` 許可リストに載せるか（→ `docs/04` §S-041 → `docs/05` §6.4 → `pick-detail.ts`） | ⑨と同じ順序で判断する。既定 = **載せる**（`tenant.purge` = 対象種別と件数〔`counts` のキー = `PURGE_SPEC.delete` の表名と数値のみ〕/ `data_export.download` = `requestId` と `format`。**返却データの内容・削除された内容には到達しない**。`F-064 AC-8`「削除の実行と返却の実施を `AuditLog` に記録する」の画面側の充足）。 | ⑨と同じ 3 箇所の更新 + 結合の正 / 負のケース green。 |
| ⑪ | SP-11 T-11-09 申し送り ④ | `S-041` の CSV エクスポートは未実装のまま。実装時はヘッダを既存 5 列 + `actorDisplayName` に固定するスナップショットを同時に置く（`docs/05` の同節。静的 `audit-detail-single-path.test.ts` の CSV 検査が `detail` / `summary` の識別子を落とす） | 🔴 **既定（`pm`）= Phase 1 で実装する**（**新スコープではない** —— `docs/04` §S-041 に CSV が定められており、`F-005` の監査ログは説明責任の対象〔`CLAUDE.md` §3.5〕で、取引先への説明に紙が要る。**T-11-09 は「未実装のまま」と申し送っただけで見送りを決めていない**。Phase 2 へ送るなら `ui-design` が §S-041 のフェーズ注記を改訂してから）。実装: ヘッダを既存 5 列 + `actorDisplayName` に固定するスナップショットを同時に置き、**`detail` / `summary` を CSV に載せない**（静的 `audit-detail-single-path.test.ts` の CSV 検査を通す）。🔴 **`T-12-17` ⑲(a) の CSV 無害化（純粋関数）を共用する**（返却 CSV と監査 CSV で 2 実装にしない）。 | 結合（CSV のヘッダ 6 列固定 / `detail` `summary` の識別子が本文に無い / 先頭 `=` の値が無害化されている）+ 静的 green。 |
| ⑫ | SP-11 T-11-09 申し送り ⑦ | （code-reviewer。2026-09-17）`admin.tenant.create` 等が接尾辞 `.create` / `.update` で `CRUD_KEYS` の評価対象になる —— `resolveAuditDetailKeySpecs` で `admin.` を接尾辞族から除外するかを決める（`docs/05` §6.4 の表に注記） | 🔴 **`program-design` の判断（`docs/05` §6.4 の表）。既定 = `admin.` を接尾辞族から除外する**（運営者の action は `S-041` の閲覧者〔テナントの `OWNER` / `ADMIN`〕に見せる対象ではなく、`A-006` の側で扱う。**誤って CRUD の許可キーが `admin.*` に適用される経路を閉じる**）。順序 `docs/05` §6.4 → `pick-detail.ts`。 | `docs/05` §6.4 に決定がある。ユニット（`admin.tenant.create` が `CRUD_KEYS` の評価対象にならない）green。 |
| ⑬ | SP-11 T-11-09 申し送り ⑥ | `U-17` の既存列 `targetId`（パートナー所有エンジニアの不透明 ID）は据え置き（[Issue #63](https://github.com/Festal-KM/SES-Platform/issues/63)）。 | 🔴 **回答待ち（[#63](https://github.com/Festal-KM/SES-Platform/issues/63)）。既定 = 据え置き。** 「伏せる」に決まった場合は `docs/04` §S-041 の `対象` 列 → `docs/05` §6.4 #10 → 実装（パートナー所有行の `targetId` を取引先向け / ホスト向けの両方で `PARTNER_LEDGER` と同じ扱いに寄せる）。 | 回答が来ていなければ「回答待ち」と本表に残す。 |
| ⑭ | SP-11 T-11-07 申し送り ③ | `demo` の `A-005` 項目 13「疑似送信の件数」は投入直後 0 件のまま（`docs/05` §13.6 の ⚠️。表示は変えない） | **既定 = 表示は変えない。** `ui-design` が `docs/04` §A-012 に「投入直後の `A-005` 項目 13 は 0 件であり、実演で送信すると増える」を 1 行注記する（営業が「壊れている」と誤読しないため）。**seed に `email_dispatches(MOCKED)` を足さない**（提案の送信は `send.*` であり `EmailDispatch` を持たない。`docs/05` §8.7 Q ⑥）。 | `docs/04` §A-012 に注記がある。 |
| ⑮ | SP-11 T-11-12 決着の末尾 ⚠️ | `docs/04` との食い違い 2 点（単価レンジ列の万円 / 円の混在、§10.3 の「§S-011 の T-06-03 注記」の所在）は `docs/05` の同節に記録。 | `ui-design` が `docs/04` を改訂する: ①§S-016 列幅配分表の単価レンジの表記を **`S-005` / `S-006` / `S-016` で統一**する（既定 = 円単位・3 桁区切りの `formatUnitPriceRange` に揃え、匿名候補の丸め後の値〔`60〜70 万円`〕は**丸めの表示形として別に定義**する —— §10.3「万円と円を混在させない」と整合させる。**丸め関数 `rounding.ts` の出力は変えない**〔`R-1` の確定値〕）②§10.3 の `S-005` 行の参照先を「§S-011 の T-06-03 注記」から **§S-010 の実装の補足**に訂正する。 | `docs/04` §S-016 / §10.3 が改訂されている。表記の統一で `S-005` / `S-006` の実装を変える場合は render テスト green（`rounding.ts` のテストは無改変）。 |
| ⑯ | SP-10 T-10-01 SP-12 への申し送り ② | **「同じ概念に別の語」の候補**（`index.test.ts` の重複一覧から。2,272 キー中、同じ値を持つ値が 278 種 / 791 キー。大半は「ホーム」「状態」「操作」のように同じ概念を同じ語で呼ぶ正常な重複）: 確認ダイアログの取消ボタンが **「やめる」（9）/「キャンセル」（5）/「戻る」（3）** の 3 語 / 再試行が **「もう一度試す」（4）/「再試行」（1。`engineerShares.loadMore.retry`）** / 不明値が **「（不明）」（4）/「不明」（1）/「—」（14）/「（なし）」（1）** / 「却下」が承認の却下（`auditLogs.detail.enum.operation.REJECT`）と新語候補の却下（`skillDictionary.candidates.reject`）の両方。いずれも境界と責任に関わる語ではないため T-10-01 では変えていない | 🔴 **`ui-design` の判断（`docs/04` §10.1 の用語集）。** 既定: 取消 = **「キャンセル」**に統一（ダイアログの慣用）/ 再試行 = **「もう一度試す」**に統一 / 不明値 = **「—」**（表のセル）と **「不明」**（文中）の 2 用法に整理 / 「却下」は承認の却下に限り、新語候補は **「採用しない」** に改める（境界と責任に関わる語〔承認〕を他の用法と共有しない）。`docs/04` §10.1 → `packages/i18n` の値の差し替え（キーは変えない。render テストのフィクスチャを追随）。 | `docs/04` §10.1 に決定がある。`packages/i18n/src/index.test.ts` の重複一覧が更新され、render テスト green。`t(` のファイル単位の減少 0 件。 |
| ⑰ | `docs/04` `designer` 申し送り 15（任意） | 改訂 12 のワイヤーフレーム再生成（任意） | **任意。** 改訂 12 で変わった画面（`S-015` の 1 表 + フィルタ / `S-016` の列幅 / `A-012` のリセット節 / `A-005` 項目 16 / 17）のうち、**実装と乖離が大きく `programmer` の入力として誤導しうるもの**だけを `node scripts/generate-wireframes.mjs --screen <ID>` で 1 枚ずつ再生成する（🔴 **`--force` での全画面再生成は課金が発生するため行わない**）。実装済みの画面は実装が正であり、再生成しないことを選んでよい。 | 再生成した画面の一覧、または「見送り（実装済みのため）」が本表に残る。 |
| ⑱ | SP-11 §5 テスト計画「外部 API のモック方針」（所見。2026-09-17 の完了確認） | SES のバウンス・苦情の Webhook はフィクスチャ（SP-04 で作成済み）を流して `A-005` に現れることを検証する。 | 🔴 **`A-005` の Phase 1 項目表（`docs/04` §A-005 / `docs/05` §16.5）にバウンス・苦情率の項目は無く、検証対象が存在しなかった**（SP-11 §5 の文は 2026-09-17 に「Phase 2 の監視項目候補」に改めた）。**`pm` の判断 = Phase 2 の監視項目候補として `docs/04` §A-005 に起こすかを `ui-design` に問う**（既定 = 起こす。`EmailEvent` の材料は SP-04 で在り、バウンス率は送信ドメインの評判〔SES の送信停止〕に直結する運用指標である。`F-059` の Phase 2 追加分〔`F-043` 未起票〕と同じ再計画で扱う）。**Phase 1 では実装しない。** | `docs/dev-plan.md` §8 に Phase 2 再計画への入力として記録されている（本行）。`ui-design` への依頼は Phase 2 再計画時。 |

- 🔴 **守ること**: ③⑨⑩⑪⑫ は監査ログ / 管理平面の応答に触れるため**境界領域として `code-reviewer` 1 回**（`CLAUDE.md` §8.3）。①②④⑤⑭⑮⑯⑰ は `docs/04` の改訂が先で、**改訂前に画面を触らない**。⑥⑦⑬ は回答待ちを理由に本タスクを止めない（既定で進む）。⑧⑱ は Phase 2 再計画への入力であり、本スプリントでは記録まで。
- ✅ ⑥⑦の Issue = [Issue #69](https://github.com/Festal-KM/SES-Platform/issues/69)（2026-09-17 起票。`assumption`。既定 = 現状維持）。`docs/05` §5.5 / §16.1 への「暫定。Issue #69 で確認中」の注記は本タスクの着手時に `program-design` が行う。
- **完了の判定**: 上表 ①〜⑱ の「完了の判定」列がすべて満たされている。**⑥⑦の Issue 番号を本節に追記する**（`docs/dev-plan.md` §9 と相互参照。どちらか一方にしか無い状態を作らない）。🔴 **⑬⑰⑱ は「回答待ち」「見送り（任意）」「Phase 2 へ」と書き残して閉じてよい**（未消化のまま消さない）。

## 5. テスト計画

| 層 | 内容 |
|---|---|
| **ユニット** | ~~本スプリントでは新規追加しない（既存の維持）。~~ **改訂（2026-09-17）**: `T-12-13`〜`T-12-16` で render / 型 / 静的テストを足す（`audit_logs.targetType` のリテラル 0 件 / `data-can-approve-now` / `PartnerHomeView` の件数示唆フィールド無し / `S-006` セクション 4・5 の render）。**改訂（2026-09-17。2 回目）**: `T-12-17` で `engineer-share-list-contract`（`PAGE_SIZE_DEFAULT` の対照）/ `admin-forbidden-keys`（API-A8 例外を `^GateStall` に限定 + `*Response` 型の区画追加）/ `proposal_request.*` の `operation` リテラル 0 件 / CSV 無害化の純粋関数 / `packages/config` の `TENANT_PURGE_GRACE_DAYS` 本番固定、`T-12-18` で `pick-detail.ts` のスナップショット更新（🔴 **キーは `docs/04` → `docs/05` の順で足したものだけ**）。**それ以外は既存の維持。** |
| 🔴 **リリース判定** | **`T-12-11` の受け入れ基準 ①〜⑨ のチェック**（自動テストではなく記録の確認である。`MODE: REVIEW` / `TARGET: Phase 1` が機械的に走査できるよう、**証跡のパスを完了記録に列挙する**）。 |
| **結合** | 環境分離の起動検証（`production` でモック → 起動失敗 / 非本番に本番キー → 起動失敗）。AI 全停止時のゲート挙動。**`T-12-13` ①の migration 後の `audit_logs` / `T-12-14` ②のゲート履歴の境界 / `T-12-15` の `F-006` の 5 種別と取引先 2 種別 / `T-12-16` の 404 条件と `engineer.view` の記録**（2026-09-17 追加）。**`T-12-17` の `usage-storage-reconcile` 前月キー / `A-005` 項目 7 の `RUNNING_OVERDUE` / 返却 CSV の無害化 / `T-12-18` の `A-002` 要約と `S-041` 許可リストの正・負のケース**（2026-09-17。2 回目）。 |
| **E2E** | 🔴 **T-12-03 の ~~18~~ ~~19~~ 20 シナリオ + T-12-04 の 5 環境 + T-12-05。** 実行ログを保存する。**#8 は `reload()` 無し（`T-12-13` ⑤）、`home.mobile.spec.ts` に要対応キューのモバイル 1 ケース（`T-12-15`）。** **#26 に `F-053 AC-3` 実演の通し（`UC-21` 順路。✅ `7cfe4d5`）、`T-12-17` で `admin.mobile.spec.ts`（新設。`A-004`）と `settings.mobile.spec.ts`（`S-041` の 1 ケース）**（2026-09-17。2 回目。`T-11-04` ⑤ の smoke は既存の `admin-non-disclosure.spec.ts` (c) で済んでいる —— `T-12-17` ⑨）。🔴 **`F-028` の環境別の検証は render テストへの読み替えを `T-12-03` に固定した**（ブラウザ経路は `development` のみ）。 |
| **負荷** | `seed:perf` に対する `F-009` / `F-015` / `gate.run` / `send.proposal` の p50 / p95 / p99。 |
| **外部 API のモック方針** | 全モック（`packages/connectors/src/mock/**` / `packages/ai/src/mock/`）+ **コンテナのネットワーク外向き遮断**。🔴 **改訂（2026-09-10）: ~~`sandbox` の分類 1 のみ MailHog で実受信を検証~~ → `sandbox` は Phase 2 へ移ったため、実受信の検証は `development` / `demo` の MailHog で行う。** **`production` は実 SES であり、モックが選ばれたら起動失敗する**（`T-12-11` ②）。 |

## 6. 完了判定（= Phase 1 の完了判定）

🔴 **次をすべて満たしたときのみ Phase 1 を完了とする。1 つでも欠ければ `PHASE_INCOMPLETE`。**

| # | 条件 | 証跡 |
|---|---|---|
| 1 | 🔴 **「案件登録 → パートナーへ公開 → パートナーが提案 → ゲート実行 → ホストが承認 → 送信 → 結果記録」を E2E で完遂できる**（`CLAUDE.md` §5） | `tests/e2e/proposal-cycle.spec.ts` シナリオ 1 の実行ログ |
| 2 | 🔴 **ゲート FAIL の提案が送信できない**（同上） | 同シナリオ 2 の実行ログ + force / override の API が存在しないことの静的テスト |
| 3 | 🔴 **共有可のエンジニアがホストの検索結果に匿名 5 項目でのみ現れ、`Proposal` が作成されるまで実名・所属会社名・スキルシートに到達できない**（同上） | `tests/e2e/anonymous-share.spec.ts` の 8 シナリオの実行ログ |
| 4 | 🔴 **R-1: 匿名 5 項目の丸め方について人間の承認が得られている**（`CLAUDE.md` §5 のリリース条件）。✅ **承認は 2026-09-10 に得た**（[Issue #5](https://github.com/Festal-KM/SES-Platform/issues/5)「OK です」/ 承認者は事業責任者単独 = [Issue #18](https://github.com/Festal-KM/SES-Platform/issues/18) 回答 ①）。🔴 **残るのは根拠の記録である**（母集団規模 / 丸め粒度 / 残存リスクの見立て。T-12-06 手順 2） | Issue #5 の承認コメント + `docs/dev-plan.md` §8 の意思決定ログ + **3 点の記録** |
| 5 | 🔴 **R-2: エンジニア 1 万件 / 案件 1 万件 / 匿名共有 2,000 件で `F-009` の p95 が 1 秒以内** | T-12-02 の測定結果（未達なら代替の要否を提起した Issue） |
| 6 | Phase 0 の成功条件（越境 0 件）が引き続き green | `tests/isolation/**` と E2E #1 / #2 の CI ログ |
| 7 | §6.1 の K-1〜K-5 / K-7 の防止機構のテストが存在し green（K-6 は Phase 2） | 各テスト名と CI ログ |
| 8 | AI・メール・ストレージの計測が動作し、件数と金額を独立に持つ | `F-026` の結合テスト |
| 9 | 🔴 **改訂（2026-09-10。[Issue #20](https://github.com/Festal-KM/SES-Platform/issues/20)）**: **`demo` が Phase 1 時点で用意され、合成データのみで実演でき、`demo` / `development` 以外では投入・リセットできない。** あわせて **`F-064`（解約時の削除と返却）が `CLOSING` → `PURGED` で成立している**（**解約は本契約テナントでも起こる**）。~~`sandbox` が用意され本番と同じ統制が効いている~~ → 🔴 **Phase 2 へ**（`T-13-11` / `T-10-08`） | `F-053` / `F-064` の E2E（**#17 と #24**。🔴 **予告が配送されるまで削除が実行されない**ことを含む。**#17 / #24 の削除側は結合 `tests/isolation/tenant-purge.test.ts` への読み替え**〔`T-12-03` の「読み替えの固定」②〕）。🔴 **`F-053` は #26 = `tests/e2e/admin-demo.spec.ts`（投入 → リセットの骨格 = `AC-2` / `AC-6`。`production` 等の 403 は結合 `seed-demo.test.ts` ⑤ / ⑦ への読み替え）+ `F-053 AC-3` 実演の通し（`UC-21` 順路。✅ 2026-09-17 `7cfe4d5`。green の確認は `T-12-03`）**（2026-09-17。SP-10 の完了確認で証跡の形を確定）。**`F-054` の E2E は Phase 2** |
| 10 | 🔴 **E-2: 本番アカウントと非本番アカウントが分かれ、非本番の認証情報で本番リソースへ到達できない**（`docs/03` §3.2.8）。🔴 **`sandbox` 用アカウントの作成は `T-13-11`（Phase 2）** —— **要求が消えたのではなく時期が後ろになっただけである** | T-12-09 のアカウント ID の対応表と拒否の確認記録（`docs/dev-plan.md` §5 E-2 / §8） |
| 11 | 🔴 **[Issue #42](https://github.com/Festal-KM/SES-Platform/issues/42)（公開後の公開欄の編集と再検査）が決着し、決定が記録されている**（推奨②を採る場合は実装とテストまで） | T-12-10 の完了判定（Issue のコメント + `docs/dev-plan.md` §8 + ② なら結合テスト / ① なら `docs/02` `F-014` と `docs/05` §11.11 の明記） |
| 🔴 **12** | 🔴 **第 1 回リリースの準備（`T-12-11`）の受け入れ基準 ①〜⑨ がすべて満たされている**（[Issue #46](https://github.com/Festal-KM/SES-Platform/issues/46)） | T-12-11 の完了記録（本番環境と `T-12-09` の参照 / `production` の起動時検証の確認記録 / 障害対応手順書のパス / 手動請求の算出根拠の確認 / 制約説明 §2.2 RL-1〜RL-5 / `staging` 後回しの扱い） |
| 🔴 **13** | 🔴 **`SP-21`（UI 基盤 = Tailwind CSS + shadcn/ui）が完了している** —— 素の CSS のまま顧客が触る画面を出さない（[Issue #43](https://github.com/Festal-KM/SES-Platform/issues/43)）。✅ **2026-09-11 に完了**（7 / 7。§7 の 13 項目すべて OK）。🔴 **残りは申し送り 2 件（`T-08-10` / `T-08-11`）の消化である** | `docs/sprints/SP-21-ui-foundation.md` §8（完了記録 + 13 項目の判定表）+ `docs/dev-plan.md` §8 の 2026-09-11 の 3 行 + **CI run `34553246355`**（E2E 35 passed。build → … → `test:isolation` → `test:e2e` の 1 ジョブ直列）+ `docs/sprints/SP-08-anonymous-share.md` §7 の 9 / 10 |

| **14** | 🔴 **SP-09 からの申し送り 11 項目（`T-12-13` に 6 / `T-12-14` に 4 / `T-12-15` に 1）+ E2E #23 前半の扱い（`T-12-14` ⑤）が消化され、Phase 1 で予定されていた未実装 2 件 —— `S-003` / `S-004` の要対応キュー（`F-006`。SP-03 T-03-06 が「Phase 1」と明記）と #46b 差分ビュー + `S-006` セクション 4・5（`F-019 AC-2`）—— が実在する**（2026-09-17 追加。SP-09 の完了確認 §8 ①②③） | `T-12-13` / `T-12-14` の各表の「完了の判定」列 + `T-12-15` の `F-006` 結合テスト + `T-12-16` の `F-019 AC-2` 結合テスト + `T-12-03` の表の #23 / #25 行の読み替え |
| **15** | 🔴 **SP-10 / SP-11 からの申し送り（`T-12-17` に 21 項目 / `T-12-18` に 18 項目）が消化され、任意・回答待ち・Phase 2 送りの項目は「見送り（任意）」「回答待ち（Issue 番号）」「Phase 2 へ（`docs/dev-plan.md` §8 の記録）」として表に残っている**（2026-09-17 追加。SP-10 / SP-11 の完了確認。**未消化のまま消えた項目が 0 件**であることが判定の中身である）。**加えて `T-12-09` ⑤（SNS の申請提出。[#62](https://github.com/Festal-KM/SES-Platform/issues/62)）と `T-12-03` #26（`F-053 AC-3` 実演の通し E2E）** | `T-12-17` / `T-12-18` の各表の「完了の判定」列（任意 4 件〔`T-12-17` ⑯⑰㉑ / `T-12-18` ⑰〕・回答待ち 2 件〔`T-12-17` ⑳ = [#67](https://github.com/Festal-KM/SES-Platform/issues/67) / `T-12-18` ⑬ = [#63](https://github.com/Festal-KM/SES-Platform/issues/63)〕・Phase 2 送り 2 件〔`T-12-18` ⑧⑱〕の扱いを含む）+ `T-12-09` の完了判定 ⑤ の記録（`docs/dev-plan.md` §5 E-17 / §8）+ `T-12-03` の表の #26 行の実行ログ。🔴 **SES のバウンス・苦情率（SP-11 §5 の所見）は Phase 2 の監視項目候補として `T-12-18` ⑱ に記録し、Phase 1 では実装しない** |

🔴 **本表の 1〜11 と 14〜15 は「Phase 1 の完了」の判定、12〜13 を加えたものが「第 1 回リリース」の判定である**（2026-09-10。[Issue #46](https://github.com/Festal-KM/SES-Platform/issues/46)。**14 は 2026-09-17 に追加** —— `F-006` / `F-019 AC-2` は Phase 1 の機能であり、欠けたまま「Phase 1 完了」とはできない。**15 は同日 2 回目の追加** —— SP-10 / SP-11 の決着行が本スプリントへ申し送った是正であり、持ち主が無いまま Phase 1 を閉じると次に見る者がいない）。**判定は同じ `MODE: REVIEW` / `TARGET: Phase 1` で行い、別手順を作らない**（`docs/dev-plan.md` §7）。

**次フェーズの前提条件の確認**（Phase 2 着手前に揃っていること）:

- SES の本番アクセス申請（E-1）が承認済み、または未承認の影響が記録されている
- 🔴 **E-2 の本番 / `sandbox` アカウント分離（T-12-09）が完了し、`docs/dev-plan.md` §5 E-2 が「分離済み」に更新されている**
- 席単価・手数料率（E-5）が Issue に再提起され、回答期限が設定されている
- 固定費の実額（E-14）が `docs/03` §7.4 に反映されている
- [Issue #3](https://github.com/Festal-KM/SES-Platform/issues/3)（マッチング重み）の督促が SP-13 着手前に出されている
- 🔴 **Phase 2 / 3 のスプリント（SP-13〜SP-20）の詳細化（再計画）を行う**（`docs/dev-plan.md` §3.4 / `PM-A-08`）
