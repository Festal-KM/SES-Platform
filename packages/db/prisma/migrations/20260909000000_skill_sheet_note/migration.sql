-- ============================================================================
-- T-05-06 スキルシートの版管理と CLEAN ゲート（docs/05 §6.4 #19 / §14 / `F-011` / `S-008`）
-- ============================================================================
-- 本 migration が置くのは 1 列だけである: `skill_sheets.note`（版のメモ）。
--
-- 🔴 なぜ列が要るか: `docs/02` `F-011` の入力は「ファイル、対象エンジニア、**版のメモ**」であり、
--    docs/05 §6.4 #19 の request も `{ objectKey, note? }` である。ところが §3.4 の
--    `SkillSheet` に保存先が無かった（設計書どうしの食い違い）。**受け取って捨てる**実装は
--    「入力したのに残らない」という、利用者からはバグと区別できない壊れ方になるため、
--    列を足して docs/05 §3.4 / §6.4 を追従させた（`CLAUDE.md` §8.7）。
--
-- 🔴 `app_platform`（運営者）には **GRANT しない**。版のメモは利用者の自由入力であり、
--    氏名・案件名・単価が書かれうる（`CLAUDE.md` §10.5「運営者に必要なのは件数・状態・エラーで
--    あって内容ではない」）。列レベル GRANT は「足さなければ見えない」= fail-closed であり、
--    ここで何もしないことがそのまま非開示になる（migration 20260904010000 の GRANT 一覧に
--    `note` を足さない。`tests/isolation/support/platform-grants.ts` の denylist が固定する）。
--
-- 🔴 `app_tenant` は `skill_sheets` に**表単位**の GRANT を持つ（migration 20260903050000 §13）
--    ため、新しい列に対する追加の GRANT は要らない。RLS（C3 OWNER_SCOPED）も表単位であり、
--    列を足しても境界は変わらない。
--
-- 🔴 `app_scan_probe`（migration 20260908000000）の列レベル GRANT にも**足さない**。
--    あちらに開いてよいのは「スキャンの 3 列」だけである。
ALTER TABLE skill_sheets ADD COLUMN IF NOT EXISTS note text;

COMMENT ON COLUMN skill_sheets.note IS
  '版のメモ（F-011 の入力 / S-008 のアップロード欄）。🔴 利用者の自由入力であり運営者に GRANT しない（T-05-06）';
